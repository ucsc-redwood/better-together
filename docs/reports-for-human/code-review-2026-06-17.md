# Code-improvement review — 2026-06-17

> Multi-agent adversarial review of the (pre-AI) C++/Python codebase, run after taking the
> full profile→z3→run→baseline pipeline end-to-end. 8 reviewers × per-finding adversarial
> verification. **48 findings raised, 46 survived.** Effort S/M/L, risk low/med/high.


## Summary

46 confirmed findings, 6 tiers. Worst cluster: z3 optimizer-feed Python (gapness-only solver, 0.0 absent-PU cost, avg-not-makespan speedup, hardcoded baselines). Next: runtime robustness (absent-PU schedule terminates a worker thread). Largest dedup: pipe/ duplication farm. Then dead code. The 7 verifier overstated-downgrades are respected.


## Recommended first move

Fix the minimize_mode dead-thread (Tier 1, item 1): thread minimize_mode end-to-end (param into solve_optimization_problem and add_chunk_time_constraint; in 02_gen_schedule_merged.py pass args.minimize_mode through) AND map CLI tmax to constraint max_time at the 02 boundary so the wired value does not trip the ValueError branch. Highest value-to-risk: it is the headline correctness defect (every tmax schedule is a byte-identical gapness clone, invalidating the tmax-vs-gapness comparison the pipeline exists to produce), yet S-effort, contained to 3 Python files (solver.py, constraints.py, 02_gen_schedule_merged.py), and touches no C++. Blast radius tiny: gapness was always the default so existing gapness results stay bit-identical. Safety net: C++ untouched so ctest --test-dir build/pc -L omp --output-on-failure stays green; add a unit test asserting gapness and tmax yield a different T_max objective on a GPU-skewed fixture (today provably identical) so it fails before and passes after; diff a regenerated gapness JSON before and after to confirm unchanged. Do this before the absent-PU 0.0 fix (M, medium risk: must preserve use_cuda selection and reach into C++ device availability) and before any pipe/ dedup (which should wait on the get_mr normalization in Tier 4).


## Tier 1 Correctness


| item | effort | risk | files |
|---|---|---|---|
| **minimize_mode never reaches solver; tmax==gapness** — Every tmax JSON is a gapness clone; tmax sweep bogus; token mismatch crashes if wired | S | low | `02_gen_schedule_merged.py:164; smt/solver.py:21; smt/constraints.py:72-77` |
| **Absent PU encoded as 0.0 cost** — 0.0 is best in a minimization; z3 schedules onto absent hardware, crash. Forbid from devices JSON | M | medium | `export_btpm_csv.py:54; smt/data_loader.py:65; smt/constraints.py:48-56` |
| **Speedup uses avg-chunk not makespan** — avg at most max so every speedup inflated, favors imbalance. Use max_time | S | low | `smt/solution_analyzer.py:216-230` |
| **Hardcoded baselines decoupled; no minipc** — btpm scored against isolated baselines; minipc gives None baseline-free JSON | M | medium | `smt/baselines.py:4-76` |
| **05_timeline.py parses obsolete log format** — Wrong UID/freq regex; frequency falls back to hardcoded 24576000, mis-scaling cycles to ms | M | low | `05_timeline.py:70-110; pipeline/record.hpp:120-142` |
| **device_specs_embedded.hpp hand-regenerated, no codegen** — Edit device JSON without rerun compiles stale topology silently; comment wrong | S | low | `conf.cpp:8-14; device_specs_embedded.hpp` |
| **BT_GIT_SHA captured at configure time** — Stale provenance sha after commit+rebuild mislabels runs | M | low | `CMakeLists.txt:195,275; pipe/bm_prof_common.hpp:164` |


## Tier 2 Robustness


| item | effort | risk | files |
|---|---|---|---|
| **No PU validation; absent tier terminates** — Empty-affinity throws in an unguarded worker thread, uncatchable terminate aborts batch | M | low | `pipeline/schedule.hpp:122-155; app.hpp:34-47; affinity.hpp:83-99` |
| **Worker threads throw with no per-thread catch** — Logger OOB on over-16 chunks throws in thread body, terminate. Wrap in try/catch | M | low | `pipe/*/const.hpp:62-95; pipeline/record.hpp:79-91` |
| **Missing-stage fallback fills zeros** — Partial CSV yields a free phantom stage and mis-balanced schedule, no nonzero exit | S | low | `smt/data_loader.py:67-70` |
| **04 makespan swallowed parse plus div-by-zero** — Malformed window widened; len-zero ZeroDivisionError aborts; widest-chunk drops non-positive | M | low | `04_parse_schedules.py:73-92; smt/model_comparison.py:225-231; smt/statistics.py:132` |
| **use_cuda sniffed from data not backend flag** — A stray nonzero cuda cell flips a Vulkan target to CUDA; use the explicit flag | S | low | `smt/data_loader.py:49,65` |
| **CUDA launch errors deferred, mis-attributed** — kSync false plus no post-launch check blames an early bad launch on the wrong stage | S | low | `tree/cuda/dispatchers.cu:28-217; cifar-cuda/all_kernels.cuh:45-111` |
| **g_vma_allocator global never nulled** — Latent UAF/double-free if two Engines coexist; null it, make it a member | M | medium | `kiss-vk/base_engine.cpp:15,45-47; kiss-vk/vma_pmr.cpp:81-149` |
| **do_allocate ignores alignment, drops cudaError** — Latent over-alignment; dropped error string hurts edge OOM diagnosis | S | low | `common/cuda/cu_mem_resource.cu:27-76` |
| **Logger drops end-at-most-start records** — Sub-resolution fast stages vanish, biasing durations upward. Use end at least start | S | low | `pipeline/record.hpp:131,152` |
| **Missing build target silent until scp-deploy** — Hand-list omission has no local signal; add aggregate target. tree-vk gap not current | S | low | `run-on-jetson.sh; CMakeLists.txt:177-272` |
| **vulkan preset omits BT_BUILD_BENCHMARKS** — MiniPC vk builds no bm binaries without flag. Caveat: android also OFF not ON | S | low | `CMakePresets.json:29,33-56` |


## Tier 3 Performance


| item | effort | risk | files |
|---|---|---|---|
| **Device-wide sync plus cub alloc-free churn** — Serializes concurrent chunks; per-item alloc raw-free never caches, lands in profiled ticks | M | medium | `tree/cuda/dispatchers.cuh:45,58; tree/cuda/dispatchers.cu:18-179` |
| **HOST_CACHED flushes all allocations per submit** — O of total buffers cache maintenance under mutex per stage inflates GPU wall-clock to z3 | M | medium | `kiss-vk/sequence.cpp:189,215; kiss-vk/vma_pmr.cpp:139-151` |
| **Per-stage descriptor+cmd re-record plus fence round-trip** — Host overhead profiled; buffers fixed per AppData so record once and replay | L | medium | `cifar-sparse/vulkan/dispatchers.cpp; kiss-vk/sequence.cpp:82-206` |


## Tier 4 Maintainability dedup


| item | effort | risk | files |
|---|---|---|---|
| **const.hpp ~95pct duplicated across 6 cells** — Any worker/queue fix needs 6 edits, already drifted. Lift to one templated common header | M | low | `pipe/*/const.hpp; pipe/bm_prof_common.hpp` |
| **pipe/ duplication farm; path to Stage registry** — Phase-4 step: const.hpp to one cell.hpp plus 6 Traits. 80pct dedup 20pct risk | M | medium | `pipe/*/const.hpp; rearchitecture.md:96` |
| **bm_gen_log/bm_fully/bm_baseline each forked ~6x** — One race fix needed 6 cells. Parameterize per family; bm_baseline off present-PU list | L | medium | `pipe/*/bm_gen_log.*; pipe/*/bm_fully_vs_normal.*; pipe/*/bm_baseline.*` |
| **get_mr address-of vs pointer forks every alloc site** — CUDA returns ref Vulkan pointer. Normalize to pointer; unblocks shared make_appdata. Prereq | M | medium | `common/cuda/manager.cuh:23; tree/vulkan engine.hpp:20; pipe/*/const.hpp:32` |
| **18 bm-* targets hand-enumerated beside a foreach** — bm-prof loops the same product; dropped line builds fine fails at deploy. Use foreach | M | low | `CMakeLists.txt:177-280` |
| **git-sha and CUDA flags duplicated across cu/vk** — Two git commands and 3-4 flag copies drift. Capture once; centralize flags | S | low | `CMakeLists.txt:146-322` |
| **Warmup magic schedule bypasses validation; dead old executor** — 2-chunk warmup unvalidated; old bm_gen_log includes a nonexistent common.hpp | M | low | `pipe/*/bm_gen_log.cpp:167-184; cifar-dense/vulkan/bm_gen_log.cpp:374-412` |
| **rearchitecture.md Phase 2 marked Next but DONE** — Load-bearing doc misrepresents state; misleads contributors. Move to Done | S | low | `rearchitecture.md:94-111; conf.cpp:26-47` |


## Tier 5 Dead code and traps


| item | effort | risk | files |
|---|---|---|---|
| **utility nnapi plus 3 probes dead; query_warpsize superseded** — Not in CMake; query_warpsize redundant with devices subgroup_size. Delete nnapi; resolve probes | S | low | `utility/nnapi/*; utility/check_vulkan.cpp; utility/query_warpsize.cpp` |
| **Orphan bm_* plus duplicate worker.hpp from pipe migration** — Six unwired orphans plus dead worker.hpp duplicate the live harness. Flag now | S | low | `cifar-dense/vulkan/bm_*.cpp; tree/*/bm_mini.cpp; pipeline/worker.hpp` |
| **00_bm.py run path dead (xmake), parser duplicated** — Run path cannot succeed; parser duplicates export_btpm_csv. Add a banner | S | low | `00_bm.py:180-201; export_btpm_csv.py:29,51` |
| **gen_schedule_tpu and 04_adv duplicates; stale README** — tpu hardcodes gapness and is broken; adv byte-identical; README names vanished scripts | S | low | `gen_schedule_tpu.py; 04_parse_schedules_adv.py; collect/README.md:63-69` |
| **CudaManager holds an unused stream; default-stream launches** — get_stream unused, per-stream attach absent. Wire fully or delete; trap for concurrent chunks | M | medium | `common/cuda/manager.cuh:17-27; tree/cuda/dispatchers.cu:28-217` |
| **CudaManagedResource broken on Tegra; prevent regression** — Pinned fix shipped but requires clause still permits the broken class. Tighten or static_assert | S | low | `common/cuda/cu_mem_resource.cu:29-91; common/cuda/manager.cuh:13-14` |
| **kiss-vk no teardown: device/instance leak, no waitIdle** — Real leak and missing teardown; fix sound. Downgraded: Tegra-crash root-cause unverified | M | medium | `kiss-vk/base_engine.cpp:44-48; sequence.cpp:29-33; algorithm.hpp:25` |


## Tier 6 Downgraded by verifier


| item | effort | risk | files |
|---|---|---|---|
| **bm_fully GPU column 3/4 reuse named index** — Overstated: 3 and 4 equal ProcessorType and table.hpp has kVukIdx/kCudIdx. Reuse them | S | low | `pipe/tree-cu/bm_fully_vs_normal.cu:249; pipe/table.hpp` |
| **Name drift plus _vk_ in .cu plus tree-vk dead block** — Real but two of four pointers wrong, _vk_ not logged. Genuine: tree-vk dead block | S | low | `pipe/tree-vk/bm_baseline.cpp; pipe/tree-vk/bm_gen_log.cpp:127-141` |
| **x86 TSC 10ms calibration minor nit not DVFS bug** — Overstated: not on hot path, invariant TSC fixed regardless of boost. Only nit: no constant_tsc check | S | low | `pipeline/record.hpp:41-55` |
| **print_chunk_stats CV/percentile not reachable** — Overstated: avg over 0 guaranteed, percentile in-bounds. Only end-equals-start drop real | S | low | `pipeline/record.hpp:205-210` |
| **Token pool off-by-one 16 into capacity-15 queue** — Benign: 15 circulate one wasted alloc. Size to pool-plus-1; fix with const.hpp dedup | S | low | `pipe/*/const.hpp:18,21; pipeline/spsc_queue.hpp:20-65` |
| **CUDA arch in 4 places DRY cleanup not divergence** — Overstated: explicit per-target override honors BT_CUDA_ARCH; toolchain 87 dead default | S | low | `CMakeLists.txt:30,145-320; cmake/jetson-aarch64.cmake:22` |


## Confirmed findings (full evidence)


### [cpp-driver-dedup] const.hpp is ~identical across all 6 cells; only using-types, kNumStages, kPoolSize, and the get_mr() arg differ (H1 CONFIRMED)  _(M/low, confirmed)_
- **evidence:** All 6 pipe/*/const.hpp are 111 lines. worker()/worker_with_record()/make_queue_from_vector() are byte-identical (modulo static-inline). make_dataset() differs only in the mr arg. Cell-specific lines: kNumStages (tree=7 at const.hpp:11, cifar=9), kPoolSize (cu=32, vk=16 at const.hpp:18), `using DispatcherT/AppDataT`, and the include block. e.g. tree-cu/const.hpp:32 `std::make_unique<AppDataT>(&disp.get_mr())` vs tree-vk/const.hpp:32 `std::make_unique<AppDataT>(disp.get_mr())`. cifar-dense-cu/const.hpp:32 even hardcodes `cifar_dense::AppData` instead of `AppDataT` (cosmetic drift).
- **claim:** ~95% of const.hpp (the worker/queue/dataset plumbing) is duplicated 6x. Any fix to worker() or the queue protocol must be applied in 6 places, and the files have already drifted (static-inline in vk only, explicit type in cifar-dense-cu only).
- **fix:** Add pipe/bm_pipeline_common.hpp holding worker/worker_with_record/make_queue_from_vector/make_dataset templated on <DispatcherT, AppDataT, kPoolSize, kNumToProcess> (mirroring the already-shipped bm_prof_common.hpp). Each const.hpp shrinks to its includes + 4 `using`/constexpr lines + `#include` of the common header. Centralize the get_mr() arg via a `make_appdata(disp)` helper specialized per backend (see separate finding).
- **verifier note:** All concrete claims verified against the 6 real files (pipe/{tree,cifar-dense,cifar-sparse}-{cu,vk}/const.hpp). All are exactly 111 lines. Pairwise diffs confirm: (1) worker(), worker_with_record(), make_queue_from_vector() are byte-identical across all 6 modulo the vk-only `static inline` qualifier; (2) make_dataset() differs only in the get_mr() arg — cu passes `&disp.get_mr()`, vk passes `disp.get_mr()`; (3) cell-specific lines are exactly kNumStages (tree=7 at line 11, cifar=9), kPoolSize (cu=32, vk=16 at line 18), the `using DispatcherT/AppDataT` lines, and the include block. The cited drift is real: tree-cu/const.hpp:32 has `std::make_unique<AppDataT>(&disp.get_mr())` vs tree-vk:32 `std::make_unique<AppDataT>(disp.get_mr())`, and cifar-dense-cu:32 hardcodes `cifar_dense::AppData` instead of `AppDataT`. The proposed-fix precedent pipe/bm_prof_common.hpp exists. Two minor refinements (do not change the verdict): the explicit-type drift is not unique to cifar-dense-cu — cifar-sparse-cu also hardcodes `cifar_sparse::AppData` at line 32 (tree-cu alone uses `AppDataT`), so that drift is slightly more widespread than implied; and the '~95% duplicated' figure is a fair estimate (~100 of 111 lines are common). Category (maintainability) and effort/risk are reasonable.

### [cpp-driver-dedup] bm_gen_log, bm_fully, bm_baseline are each ~6x duplicated, differing only in per-app namespace tokens and per-backend GPU-timing/column tokens (H2 CONFIRMED)  _(L/medium, confirmed)_
- **evidence:** bm_gen_log: 5 of 6 cells are 242 lines; cu-vs-cu diff is 6 lines (only the `tree::omp::`→`cifar_*::omp::` token, a function NAME, a comment); cu-vs-vk diff is the kCuda↔kVulkan token swap in the dispatch branch (gen_log GPU call `disp.dispatch_multi_stage(*app,start,end)` is byte-identical across all 6, line :44/:111). bm_fully cu-vs-cu diff is ~6 namespace/comment lines; cu-vs-vk diff is purely cudaEvent-timing vs std::chrono-timing plus cud_/vul_ renames. bm_baseline cu-vs-cu diff is 7 lines (namespace + stage count 7/9 + BENCHMARK Name). Confirmed end-to-end: every md5 differs but the structural body is shared.
- **claim:** The three driver families are copy-paste forks. Two real bugs already fixed in commits f2466aa/76fdc35 (the disjoint-AppData race) had to be patched per-cell; the next such fix will again need 6 edits. Drift is already present (see stale-comment finding).
- **fix:** Factor each family into one parameterized header: (a) a CPU OMP-dispatch wrapper passed from const.hpp removes the `<app>::omp::` token; (b) a backend trait (GPU timer = cudaEvent vs chrono, gpu column index, pu enum) removes the cu/vk token swaps. bm_gen_log and bm_baseline-cu collapse almost completely; bm_fully keeps a ~30-line backend-timer trait. Estimated ~4.7k lines -> ~1k as hypothesized (H3), with every future fix applied once.
- **verifier note:** Verified against the real source under pipe/*/ (the build/ .o files are artifacts; sources are the 18 files = 3 families x 3 apps x {cu,vk}).

bm_gen_log: confirmed 5 of 6 cells are exactly 242 lines (tree-vk is 257). cu-vs-cu diff (tree vs cifar-dense) is exactly the `tree::omp::`->`cifar_dense::omp::` token, a function NAME (BM_pipe_*_vk_schedule_auto), and an xmake comment line. cu-vs-vk diff is the kCuda<->kVulkan token swap in the dispatch branch plus the "Don't run X in Y mode" string and a comment. The GPU dispatch call `disp.dispatch_multi_stage(*app, start, end)` is byte-identical across all 6 at lines 44 and 111. md5s all differ. Confirmed.

bm_fully cu-vs-cu (tree vs cifar-dense): differences are purely the namespace token (multiple call sites), a stale comment block, one line-wrap, and the xmake comment. cu-vs-vk: purely cudaEvent-timing vs std::chrono-timing plus cud_/vul_ renames. Confirmed in kind. Minor: the finding says "~6 lines" but the actual changed-line count is 17 (all of the claimed kinds) -- an understatement of magnitude, not of character.

bm_baseline cu-vs-cu: exactly 7 hunks = namespace token + stage count (1,7 vs 1,9) + BENCHMARK Name string + xmake comment. Exact match to the finding's "7 lines (namespace + stage count 7/9 + BENCHMARK Name)".

The per-cell-bug-fix claim is concretely validated by git history: the disjoint-AppData race fix was applied as an identical "15 +++++----" patch to tree-cu/tree-vk bm_fully in f2466aa, then re-applied to all 4 cifar bm_fully cells in 76fdc35 -- 6 cells, one logical fix. const.hpp exists per cell, so the proposed parameterized-header fix premise is sound.

Two minor numeric overstatements that do NOT affect the conclusion: (1) total source is ~4007 lines, not ~4.7k, so the H3 "~4.7k -> ~1k" estimate is inflated on the source side (build artifacts likely counted in the original); (2) bm_fully cu-vs-cu is 17 changed lines, not ~6. Core maintainability finding (copy-paste forks, per-cell fixes, real drift) is real and correctly characterized.

### [cpp-driver-dedup] AppData::get_mr() vk-vs-cu signature divergence is real and recurs at EVERY allocation site, not just const.hpp (H confirmed)  _(M/medium, confirmed)_
- **evidence:** CUDA dispatchers return a memory_resource by value/ref requiring address-of: const.hpp:32 `std::make_unique<AppDataT>(&disp.get_mr())`; same `&disp.get_mr()` in tree-cu/bm_prof.cu:62, :101, :109. Vulkan dispatchers take it directly: tree-vk/const.hpp:32 `std::make_unique<AppDataT>(disp.get_mr())`. This `&` vs no-`&` is the single most pervasive cu/vk token and appears in make_dataset, every bm_prof background-app alloc, and the registered-benchmark body.
- **claim:** This one API inconsistency forces a fork at every AppData construction and is the main thing blocking a single shared `make_appdata(disp)` helper. Unifying get_mr()'s return convention (both return the same `T*` or both return `T&`) removes the bulk of cu/vk divergence in const.hpp and bm_prof, leaving only the genuinely-different GPU-timing path.
- **fix:** Normalize the Dispatcher API: have both backends expose `get_mr()` returning the same shape (prefer a pointer, matching CUDA call sites), OR add a thin `inline auto* mr_of(DispatcherT&)` adapter per backend in const.hpp and route all `make_unique<AppDataT>(mr_of(disp))` through it. Then the dataset/appdata factory is backend-agnostic and the shared header from finding 1 needs no per-backend specialization for allocation.
- **verifier note:** Verified against the cited code. Root cause is real: CUDA's get_mr() returns a reference (builtin-apps/tree/cuda/dispatchers.cuh:17 `CudaPinnedResource &get_mr()`, builtin-apps/common/cuda/manager.cuh:23 `MemResourceT &get_mr()`), so CUDA call sites need `&disp.get_mr()`; Vulkan's get_mr() already returns a pointer (builtin-apps/tree/vulkan/dispatchers.hpp via engine.hpp:20 `VulkanMemoryResource* get_mr()`), so Vulkan call sites pass `disp.get_mr()` directly. Both AppData ctors take a pointer (`std::pmr::memory_resource*`): tree/safe_tree_appdata.hpp:14 and tree/vulkan VkAppData_Safe. The `&` vs no-`&` evidence matches exactly: tree-cu/const.hpp:32 `make_unique<AppDataT>(&disp.get_mr())` vs tree-vk/const.hpp:32 `make_unique<AppDataT>(disp.get_mr())`; tree-cu/bm_prof.cu:62 `AppDataT app(&disp.get_mr())`, :101 and :109 `make_unique<AppDataT>(&disp.get_mr())`. The divergence does recur at every allocation site (make_dataset + every bm_prof bg-app + the registered-benchmark body), and the proposed fix (normalize get_mr() to return a pointer in both backends, matching the ctor's pointer param) is correct and low-risk since the ctors already accept a pointer. The claim is appropriately scoped — it explicitly leaves the genuinely-different GPU-timing path (cudaEvent) and other cu/vk differences (distinct AppDataT types, includes, static inline qualifiers) out of scope, so it is not overstated. Note these are two separate const.hpp files (pipe/tree-cu/ and pipe/tree-vk/), not one file as the title's parenthetical might suggest, but the finding treats them as the two backend forks, which is accurate.

### [cpp-driver-dedup] Backend GPU column index in update_full_table is a magic constant (CUDA=4, Vulkan=3) — a real semantic difference and a naive-factoring TRAP  _(S/low, overstated)_
- **evidence:** bm_fully cu cells write the GPU result to column 4: tree-cu/bm_fully_vs_normal.cu:249 `if (cud_count>0) table.update_full_table(stage-1, 4, cud_time);`; vk cells write column 3: tree-vk/bm_fully_vs_normal.cpp:235 `update_full_table(stage-1, 3, vul_time)`. The four CPU columns (0/1/2 for lit/med/big) are shared. update_normal_table instead uses `static_cast<int>(pt)` (the ProcessorType enum) so CUDA and Vulkan map to DIFFERENT enum values there too.
- **claim:** If someone factors bm_fully into one header and forgets to parameterize this column, the merged BTPM table fed to z3 silently mislabels GPU timings (CUDA written where Vulkan is expected or vice-versa), corrupting the schedule with zero compile error. This is the single most important per-cell difference that breaks a careless shared-header refactor.
- **fix:** Capture the GPU column (and the ProcessorType enum value) in a per-backend trait struct used by the shared bm_fully header, e.g. `struct CudaBackend { static constexpr int kGpuCol = 4; ... }` / `struct VkBackend { static constexpr int kGpuCol = 3; ... }`. Add a static_assert tying kGpuCol to the ProcessorType so the two can't drift.
- **verifier note:** The factual core is real but the cited paths are wrong (actual files are under pipe/, e.g. /home/doremy/Desktop/ucsc/better-together/pipe/tree-cu/bm_fully_vs_normal.cu:249 and pipe/tree-vk/bm_fully_vs_normal.cpp:235), and the "magic constant" framing is overstated.

Verified facts: in bm_fully, CUDA writes update_full_table(stage-1, 4, cud_time) (cu:249) and Vulkan writes ...,3,vul_time) (vk:235); CPU columns 0/1/2 are shared (lit/med/big). update_normal_table uses static_cast<int>(pt) (cu:110, vk:103). So yes, CUDA and Vulkan land in different columns/enum values.

Where the finding overstates: 3 and 4 are NOT arbitrary magic numbers. In builtin-apps/conf.hpp the enum is ProcessorType{kLittleCore=0,kMediumCore=1,kBigCore=2,kVulkan=3,kCuda=4,...}, and pipe/table.hpp already defines named indices kLitIdx=0,kMedIdx=1,kBigIdx=2,kVukIdx=3,kCudIdx=4 for the very same 5-wide arrays. Column index == ProcessorType enum value by construction. The full table and normal table therefore use the SAME mapping — normal expresses it as static_cast<int>(pt), full hardcodes the literal that equals it. There is no semantic divergence between the two backends' column choice and the enum; CUDA-at-4 / Vulkan-at-3 is the intended, consistent layout (confirmed by table.hpp's CSV header "stage,little,medium,big,vulkan,cuda").

So the real (minor) smell is a maintainability one: the bm_fully callsites use bare literals 3/4 instead of kVukIdx/kCudIdx or static_cast<int>(pt). The proposed per-backend trait + static_assert is a fine defensive fix, but the simplest correct fix is to reuse the existing named index / enum cast. The "single most important per-cell difference that breaks a careless shared-header refactor" / "silently mislabels GPU timings" framing is speculative (no such refactor exists) and overstates a low-risk, enum-aligned literal.

### [cpp-driver-dedup] bm_baseline-cu and bm_baseline-vk are STRUCTURALLY different (cu=64 lines OMP+CUDA only; vk=139-164 lines adds per-tier + super-cores) — not a token-swap  _(M/low, confirmed)_
- **evidence:** bm_baseline.cu files are 64 lines and contain only BM_baseline_omp + BM_baseline_cuda. bm_baseline.cpp (vk) files are 139-164 lines and additionally contain per-tier LittleCores/MidCores/BigCores variants (using the 4-arg `dispatch_multi_stage(LITTLE_CORES, appdata, 1, N)` overload) and, in cifar-*-vk ONLY, a SuperCores variant (cifar-dense-vk/bm_baseline.cpp:107-127). tree-vk and cifar-sparse-vk lack super-cores; only the two cifar vk cells have it (`grep -l SUPER_CORES` matches only cifar-dense-vk, cifar-sparse-vk).
- **claim:** bm_baseline cannot be factored by a single backend trait: the cu and vk variants measure different sets of PUs, and even within vk the super-cores benchmark is present in 2 of 3 cells. A naive 'parameterize by backend token' refactor would either drop the vk-only per-tier/super-core benchmarks or add bogus per-tier benchmarks to the cu binaries.
- **fix:** Express bm_baseline as a shared header that registers benchmarks from a present-PU list (reuse bt_prof::present_cpu_pus() + has_sup_cores() guards already in the codebase) rather than hardcoding tiers per cell; the GPU benchmark is added via the backend trait. This makes super-cores appear automatically on any device that has them and removes the cu/vk structural fork.
- **verifier note:** All concrete claims verified against the real files. Line counts are exact: pipe/{tree,cifar-dense,cifar-sparse}-cu/bm_baseline.cu = 64 lines each, containing ONLY BM_baseline_omp + BM_baseline_cuda (read tree-cu/bm_baseline.cu in full). The vk cells are larger: pipe/tree-vk/bm_baseline.cpp = 139, cifar-dense-vk = 163, cifar-sparse-vk = 164. The vk cells add per-tier baseline variants using the 4-arg dispatch_multi_stage(LITTLE_CORES/MEDIUM_CORES/BIG_CORES, appdata, 1, N) guarded by has_lit/med/big_cores(). SUPER_CORES appears ONLY in the two cifar-vk cells (grep -l confirms exactly cifar-dense-vk + cifar-sparse-vk); tree-vk has the three tiers but no super-cores. So the structural fork is real and asymmetric on three axes: cu-vs-vk (per-tier absent in cu), and within-vk (super-cores in 2 of 3). One precision note that does not undercut the finding: the per-tier and super-core benchmarks are OMP benchmarks (they call <app>::omp::dispatch_multi_stage with a core-tier argument and are Named 'OMP/.../LittleCores' etc.), not Vulkan-tier benchmarks; the single Vulkan benchmark is BM_baseline_vk. The finding's evidence ('adds per-tier ... variants') is consistent with this and does not misattribute them to Vulkan. The proposed fix is grounded in real, already-used APIs: bt_prof::present_cpu_pus() exists at pipe/bm_prof_common.hpp:136 and has_sup_cores() at builtin-apps/app.hpp:23, both already consumed by the bm_prof cells. Conclusion: real, correctly characterized maintainability finding; the 'not a token-swap' claim and the risk that a naive backend-token parameterization would drop vk-only per-tier/super-core benchmarks (or add bogus per-tier ones to cu) is accurate.

### [cpp-driver-dedup] Copy-paste drift already shipped: stale benchmark Names, a `_vk_` function name inside .cu files, and dead experiment comments in tree-vk only  _(S/low, overstated)_
- **evidence:** cifar-dense-vk/bm_baseline.cpp:52,77,102 label benchmarks `"OMP/CIFAR-Sparse/Baseline/LittleCores"` etc. (says Sparse in the DENSE cell). tree-vk/bm_baseline.cpp:27 Name is bare `"OMP/Baseline"` while sparse/dense have qualified names — inconsistent. In the CUDA gen_log, the schedule fn is named `BM_pipe_cifar_dense_vk_schedule_auto` (tree-cu/bm_gen_log.cu:76 is `_tree_vk_`) — a `_vk_` name baked into a .cu CUDA file. tree-vk/bm_gen_log.cpp:127-141 carries ~15 lines of commented-out experiment code (fake sleep-based timing) absent from the other 5 gen_log cells.
- **claim:** These are pure copy-paste artifacts proving the forks have already diverged in ways that mislabel data (Name strings end up in benchmark output / logs) and accumulate dead code unevenly. They confirm the duplication is actively rotting, and any factoring should normalize the Name strings and delete the tree-vk dead block.
- **fix:** When centralizing, derive the benchmark Name from the app/backend strings already passed to bt_prof helpers (app + backend), so labels can't drift. Delete the commented experiment block at tree-vk/bm_gen_log.cpp:127-141. Pre-existing dead code per repo policy: flag, don't delete outside the refactor.
- **verifier note:** The underlying phenomenon (copy-paste drift: mislabeled benchmark Names, _vk_ fn names in .cu files, an uneven dead-code block) is REAL, but the finding misattributes two of its four specifics to the wrong files. (1) WRONG FILE: it claims cifar-dense-vk/bm_baseline.cpp:52,77,102 say "OMP/CIFAR-Sparse/..." — they actually say "OMP/CIFAR-dense/Baseline/..." (correct). The bogus "OMP/CIFAR-Sparse/Baseline/{LittleCores,MidCores,BigCores}" labels are in tree-vk/bm_baseline.cpp:52,77,102 (Sparse label inside the TREE cell). (2) INVERTED: tree-vk:27 is NOT bare "OMP/Baseline" — it is "OMP/Tree/Baseline" (qualified). The bare unqualified names "OMP/Baseline" (:27) and "VK/Baseline" (:145) are in cifar-dense-vk/bm_baseline.cpp. (3) CONFIRMED + broader: the static fn BM_pipe_..._vk_schedule_auto carries _vk_ in ALL THREE .cu files (tree-cu:76, cifar-dense-cu:76, cifar-sparse-cu:76), not just tree-cu. This is an internal static fn name; it is NOT emitted to logs/output, so it is cosmetic only. (4) CONFIRMED: tree-vk/bm_gen_log.cpp has a ~13-line commented-out sleep-based fake-timing experiment block (line 127 + lines 129-141) absent from the other 5 gen_log cells. The Name() strings are GoogleBenchmark display labels that DO appear in benchmark output, so the tree-vk Sparse mislabel genuinely corrupts reported data — but the finding's evidence pins it on the wrong cell. Proposed fix direction (derive Names from app/backend, delete tree-vk dead block) is sound. Net: real category, correct conclusion, but two of the cited file:line evidence pointers are factually wrong.

### [optimizer-feed] Absent PU encoded as 0.0 cost makes z3 schedule stages onto hardware the device lacks (CONFIRMED hypothesis 1)  _(M/medium, confirmed)_
- **evidence:** export_btpm_csv.py:54 writes `d.get(c, 0.0)` -> a PU the device lacks becomes literal 0.0 in the CSV (the docstring lines 14-15 even call this out as intentional 'absent = 0.0'). data_loader.py:38 does `df.groupby('stage')[[little,medium,big,vulkan,cuda]].mean()` and lines 65-66 build `[row['little'], row['medium'], row['big'], gpu_time]` with no notion of availability. In constraints.py:48-56 the per-segment cost is `Sum(RealVal(stage_timings[k][...]))` and `Implies(segment_assigned, seg_sum <= T_max)`; a 0-cost PU trivially satisfies the T_max bound and (in gapness mode) is attractive. The MiniPC is Big-only (devices/minipc.json: all 16 cores type 'big'; app.hpp:20-23 g_lit_cores/g_med_cores are empty there), so a Little/Medium assignment maps via config_reader.hpp:88-93 to kLittleCore/kMediumCore and app.hpp:34-47 get_cores_by_type returns an EMPTY vector -> the runtime crash you observed.
- **claim:** z3 treats an absent PU as a zero-latency (infinitely fast) unit and will assign real stages to a PU the target physically does not have, producing an unrunnable schedule. The 0.0 sentinel is fundamentally wrong: 0.0 is the *best* possible value in a minimization, exactly inverting the intended 'do not use' meaning.
- **fix:** Mark absent PUs unavailable, not 0.0. Cleanest: in export, omit absent PU columns or write a sentinel that data_loader maps to 'no variable'; in data_loader, drop the (i,c) decision variable (or add `opt.add(Not(x[(i,c)]))`) for any PU whose column is NaN/empty for the device. Do NOT use +inf as a RealVal either (z3 has no inf and a huge constant distorts T_max); structurally forbidding the assignment is correct. Source availability from devices/<device>.json (has_lit/med/big_cores) rather than from data presence.
- **verifier note:** Every cited line checks out. export_btpm_csv.py:54 writes `f"{d.get(c, 0.0):.6f}"` so a PU the device lacks becomes literal 0.0 in the CSV; the docstring (lines 13-15) explicitly calls this intentional ("0.0 for a PU the device lacks ... matching the legacy 'absent = 0.0'"). data_loader.py:38 groups/means the five PU columns with no availability notion, and lines 64-66 build `[row['little'], row['medium'], row['big'], gpu_time]` — a 0.0 column is indistinguishable from a genuinely fast PU. constraints.py:48-56 wraps each segment cost in `RealVal(stage_timings[k][...])` and adds `Implies(segment_assigned, seg_sum <= T_max)`; a 0-cost PU trivially satisfies any T_max>0 bound, and since the objective minimizes Gapness/T_max, 0.0 is the *best* value — exactly inverting the intended "do not use." The C++ crash path is also confirmed: minipc.json has all 16 cores type "big" (no little/medium), config_reader.hpp:88-93 maps a "Little"/"Medium" schedule chunk to kLittleCore/kMediumCore, and app.hpp:34-47 get_cores_by_type returns the (empty) g_lit_cores/g_med_cores by reference — an empty affinity vector for that chunk. So z3 can and will emit a schedule assigning stages to a PU the MiniPC physically lacks. Two minor refinements (do not change the verdict): (1) The proposed fix overstates one detail — data_loader's `use_cuda = avg_df['cuda'].sum() > 0` already relies on the 0.0 sentinel to pick Vulkan vs CUDA, so naively dropping/NaN-ing columns must preserve that GPU-selection behavior. (2) constraints.py:36 forces `T_min > 0` and line 66 requires each maximal segment's seg_sum >= T_min, so z3 cannot collapse *all* chunks to zero; but a single absent-PU chunk interleaved with real ones still trivially passes its `<= T_max` bound and adds zero gapness pressure, so the "attractive, produces an unrunnable schedule" conclusion stands. The 0.95 confidence and category=correctness are appropriate.

### [optimizer-feed] minimize_mode (gapness/tmax) is parsed and put in the filename but NEVER reaches the solver -- tmax and gapness schedules are identical, and 'tmax' would even be an invalid mode  _(S/low, confirmed)_
- **evidence:** 02_gen_schedule_merged.py:115 reads `minimize_mode = args.minimize_mode` and line 151 bakes it into the output filename `schedules_{table_type}_{minimize_mode}.json`, but line 164 calls `solve_optimization_problem(stage_timings, args.num_solutions, app)` with NO mode argument. solver.py:21 signature has no mode param and solver.py:39 calls `add_chunk_time_constraint(opt, x, core_types, num_stages, stage_timings_data)`, so constraints.py:28 uses the default `minimize_mode='gapness'` always. Worse: the CLI choices are ['gapness','tmax'] (line 66) but constraints.py:72-77 only accepts 'gapness'/'max_time' and raises ValueError otherwise -- so even if the string were threaded through, 'tmax' would crash.
- **claim:** Every `schedules_*_tmax.json` is byte-identical to its `schedules_*_gapness.json` (both gapness-optimized). 03_run_schedule.py:108-110 then dutifully loads the 'tmax' file believing it minimizes makespan; the entire tmax arm of your sweep silently measured gapness schedules. For a GPU-dominated app this is exactly the regime where tmax (minimize the bottleneck chunk) and gapness diverge, so this likely explains a chunk of the 'GPU app loses' symptom.
- **fix:** Thread the mode through: add `minimize_mode` param to solve_optimization_problem and pass to add_chunk_time_constraint; and reconcile the vocabulary -- map CLI 'tmax' -> constraints' 'max_time' (or rename the constraint branch to 'tmax'). Add an assert/test that gapness and tmax outputs differ on a GPU-heavy fixture.
- **verifier note:** Every cited line checks out exactly. 02_gen_schedule_merged.py:115 reads minimize_mode from args; line 151 bakes it into the output filename schedules_{table_type}_{minimize_mode}.json; but line 164 calls solve_optimization_problem(stage_timings, args.num_solutions, app) with NO mode argument. solver.py:21 signature is solve_optimization_problem(stage_timings, num_solutions=30, app_name=None) — no mode param — and solver.py:39 calls add_chunk_time_constraint(opt, x, core_types, num_stages, stage_timings_data) without a mode, so constraints.py:28 default minimize_mode='gapness' always wins and constraints.py:73 hardcodes opt.minimize(Gapness). solver.py:45 even prints 'Minimizing the gap...' unconditionally. The vocabulary-mismatch claim is also true: CLI choices are ['gapness','tmax'] (line 64) but constraints.py:72-77 only accepts 'gapness'/'max_time' and raises ValueError otherwise, so threading 'tmax' through verbatim would crash. 03_run_schedule.py:107-109 loads schedules_{table_type}_{minimize_mode}.json, so the tmax arm of the sweep loads files that are content-identical to the gapness arm (only filename/console prints differ). The 'tmax and gapness schedules are identical' claim is therefore correct. The proposed fix (thread mode through + map CLI 'tmax' -> 'max_time') is sound. One caveat: the 'explains a chunk of the GPU-app-loses symptom' is a plausible but unverified causal hypothesis (correctly hedged with 'likely'); it does not affect the verified correctness of the defect itself.

### [optimizer-feed] Reported speedup uses avg-chunk-time, not pipeline makespan, so all speedups are inflated and don't reflect steady-state throughput  _(S/low, confirmed)_
- **evidence:** solution_analyzer.py:216-230 computes `speedup_over_cpu = baseline_data['omp'] / avg_time` and `speedup_over_gpu = baseline_data[gpu]/avg_time`, where avg_time (line 152) is `sum(chunk_times)/len(chunk_times)` -- the mean over chunks. A software pipeline's steady-state per-frame time is gated by the SLOWEST stage/chunk (max_time, line 150 / the solver's T_max), not the average. The solver even optimizes around T_max, but the reported KPI divides by avg_time.
- **claim:** avg_time <= max_time always, so every reported speedup is optimistic; the more imbalanced the schedule the bigger the lie. This directly biases solution ranking/selection toward imbalanced schedules and makes cross-app comparisons (incl. 'GPU app loses') untrustworthy. The pipeline throughput baseline comparison is conceptually wrong.
- **fix:** Use max_time (the bottleneck / T_max) as the pipeline throughput denominator for speedup_over_cpu/gpu, and keep avg_time only as a diagnostic. Optionally report both pipeline-throughput speedup and latency (sum of chunks) speedup explicitly so the metric's meaning is unambiguous.
- **verifier note:** Verified against /home/doremy/Desktop/ucsc/better-together/scripts/collect/smt/solution_analyzer.py. Lines 219-221 and 228-230 compute speedup_over_cpu = baseline['omp']/avg_time and speedup_over_gpu = baseline[gpu]/avg_time. avg_time (line 152) = sum(chunk_times)/len(chunk_times), the mean over chunks; max_time (line 150) = max(chunk_times). Chunks are contiguous runs of stages on one core type (lines 101-145), i.e. the pipeline stages, so steady-state per-frame throughput is gated by the slowest chunk (max_time / T_max), not avg_time. Since avg_time <= max_time always, dividing by avg_time inflates the reported speedup whenever the schedule is imbalanced (equal only in the perfectly-balanced case, which the finding's '<=' correctly allows). The metric ignores imbalance entirely, so it biases ranking toward imbalanced schedules and makes cross-app throughput comparisons unsound. The analyzer already records max_time as a metric, so the proposed fix (use max_time as the throughput denominator, keep avg_time as a diagnostic) is straightforward and low-risk. Finding is real and correctly characterized.

### [optimizer-feed] Baselines are a hardcoded dict decoupled from the CSV table actually used; MiniPC has NO baseline entry, and btpm schedules are scored against fixed isolated-ish numbers (CONFIRMS hypothesis 2)  _(M/medium, confirmed)_
- **evidence:** baselines.py:4-61 is a literal hand-maintained dict keyed by device ('jetson','jetsonlowpower', two phone serials) and app-backend; `grep -c minipc` = 0, so for --device minipc get_baseline_for_config (baselines.py:64-76) hits KeyError -> prints 'No baseline found' and returns None, and dump_solutions_as_json (solution_analyzer.py:208-230) then writes NO baseline/speedup fields at all. The same fixed baseline numbers are attached regardless of whether --table_type was 'isolated' or 'btpm' (02_gen_schedule_merged.py:119 fetches baseline before/independent of table choice at line 130). So a btpm (under-interference) schedule is compared against an isolated-style hardcoded baseline.
- **claim:** (2) confirmed and worse than stated: the table choice (btpm vs isolated) feeding z3 is independent of the baseline the result is scored against, so the comparison is apples-to-oranges -- a GPU-dominated app scheduled with inflated interference times can 'lose' to an isolated baseline that never saw interference. And hardcoded baselines silently desync from re-profiled data and omit whole devices (minipc), yielding baseline-free JSON with no speedup at all.
- **fix:** Make the table choice explicit and consistent: derive the baseline from the SAME store (isolated.csv min-of-PU per the chosen metric), not a hardcoded dict; or at minimum require an explicit --baseline-table and assert it matches the scoring intent. Add minipc (and any new device) or compute baselines from CSV so devices can't silently fall through to None.
- **verifier note:** All cited claims verified against the real code. baselines.py:4-61 is a literal hand-maintained dict keyed by device ('3A021JEHN02756','9b034f1b','jetson','jetsonlowpower') + '{app}-{backend}', wholly decoupled from any CSV. `grep -c minipc baselines.py` = 0, so --device minipc hits the except KeyError at lines 74-76, prints a warning and returns None; dump_solutions_as_json (solution_analyzer.py:208-230) is entirely guarded by `if baseline_data and ...`, so a None baseline yields JSON with no baseline/speedup fields. The core hypothesis-2 claim holds: in 02_gen_schedule_merged.py main(), baseline_data is fetched at line 119 from only (device, app, backend); table_type is used solely to pick the CSV filename at line 130 ('{table_type}.csv') and is never passed to baseline lookup (baselines.py has zero references to table_type/isolated/btpm). Hence a btpm (under-interference) schedule is scored against the same fixed hardcoded baseline as an isolated one — genuinely apples-to-oranges. Only nit: the quoted message 'No baseline found' is slightly paraphrased — the actual strings are 'Warning: No baseline found for {device}/{app}/{backend}' (baselines.py:75) and 'No baseline data available for ...' (02_gen_schedule_merged.py:127); substance unaffected. Files: /home/doremy/Desktop/ucsc/better-together/scripts/collect/smt/baselines.py, /home/doremy/Desktop/ucsc/better-together/scripts/collect/smt/solution_analyzer.py, /home/doremy/Desktop/ucsc/better-together/scripts/collect/02_gen_schedule_merged.py. Proposed fix (compute baseline from the same store / require explicit baseline table; add minipc or derive from CSV) is appropriate.

### [optimizer-feed] use_cuda = (cuda column sum > 0) is a brittle heuristic that silently picks the wrong GPU backend when both columns are populated or a value is missing  _(S/low, confirmed)_
- **evidence:** data_loader.py:49 `use_cuda = avg_df['cuda'].sum() > 0`; lines 65 picks `row['cuda'] if use_cuda else row['vulkan']` for the single GPU slot. The --backend CLI arg (02_gen_schedule_merged.py:50, choices vk/cu) is ALREADY the source of truth but is ignored for this decision -- it's only used for the CSV path and baseline key. export_btpm_csv.py:31 emits one backend dir at a time (vulkan->vk, cuda->cu), so normally only one of the two columns is nonzero, but nothing enforces that; a stray nonzero cuda cell (or a future merged CSV) flips the whole app to CUDA on a Vulkan-only target.
- **claim:** Backend selection is inferred from data presence rather than from the explicit --backend flag the user already passed, so it can disagree with the requested backend and produce a schedule whose GPU chunks target a backend the device/binary doesn't run. It's a needless second source of truth.
- **fix:** Drive the GPU column from the explicit --backend argument (vk->vulkan, cu->cuda) and pass it into load_csv_and_compute_averages, instead of the sum>0 sniff. Keep the sum>0 check only as a sanity assertion (warn if the chosen column is all-zero).
- **verifier note:** All cited code verified. data_loader.py:49 `use_cuda = avg_df["cuda"].sum() > 0`; line 65 `gpu_time = row["cuda"] if use_cuda else row["vulkan"]`; line 158/161 in 02_gen_schedule_merged.py derive gpu_backend from this sniff. The explicit --backend arg (choices vk/cu, line 50) IS already the source of truth but is only used for the CSV path (line 131) and baseline key (line 119); it is never passed to load_csv_and_compute_averages. export_btpm_csv.py confirms one backend dir per file with 0.0 for absent PUs (line 54), and its own docstring (line 15) admits data_loader's use_cuda test relies on "absent = 0.0". So the redundant-second-source-of-truth claim is real and correctly characterized; the proposed fix is sound and low-risk. Two caveats that bound severity (the finding already hedges these): (1) In the normal pipeline the CSV path itself routes by --backend (.../vk/... vs .../cu/...) and the exporter zeroes absent PUs, so today the sniff agrees with --backend; the disagreement is latent, requiring a malformed/merged/hand-edited CSV the current exporter does not produce -- it is a robustness gap, not an active wrong-backend bug. (2) The "value missing" sub-claim is weaker than stated: a missing cell -> NaN -> `NaN > 0` is False -> falls through to Vulkan (deterministic, not silent corruption). The central point -- backend inferred from data presence rather than the explicit flag the user already passed -- is accurate.

### [optimizer-feed] Missing-stage fallback fills [0,0,0,0], injecting a free stage that z3 will gladly absorb and silently corrupting the schedule  _(S/low, confirmed)_
- **evidence:** data_loader.py:67-70: when a stage index is absent from the grouped CSV it prints 'Warning: Stage N not found' and appends `[0.0, 0.0, 0.0, 0.0]`. That stage then has 0 cost on every PU in constraints.py:48-56, so it's free wherever placed and contributes nothing to T_max/T_min. Combined with the contiguity/coverage requirement (the C++ side validate_schedule_coverage in schedule.hpp:122-155 requires full [1,n] coverage), a 0-cost phantom stage produces a schedule that runs a real kernel with no profiled cost basis.
- **claim:** A partially-collected CSV doesn't fail the run; it produces a confident schedule that treated the missing stage as free, mis-balancing the pipeline. A warning printed to stdout is easily lost in the 03_run pipeline and there is no nonzero exit.
- **fix:** Treat a missing stage as a hard error (raise) rather than zero-fill, or require the loader to assert every stage in [1,num_stages] is present before solving. If a fallback is truly needed, it must be 'unavailable', never 0.0.
- **verifier note:** All three citations verified accurate. data_loader.py:67-70: a stage index absent from the grouped CSV prints "Warning: Stage N not found in data" and appends [0.0, 0.0, 0.0, 0.0] — no raise, no exit. constraints.py:48-56: seg_sum is built from stage_timings[k][core], so a 0-cost stage adds 0 to every segment on every PU; it is genuinely free wherever placed and contributes nothing to T_max (and would push a phantom-only maximal segment to seg_sum=0, which the T_min>0 floor would reject, but in practice phantoms get folded into real chunks). schedule.hpp:122-155 validate_schedule_coverage checks only contiguous [1,n] coverage / no gaps-overlaps — it has zero notion of cost basis, so a chunk containing a phantom stage passes validation and the executor runs the real kernel. The claim of "no nonzero exit" is correct: the only caller, scripts/collect/02_gen_schedule_merged.py:157-177, wraps the loader in try/except that exit(1)s on exceptions, but the loader never raises for missing stages — so a partial CSV silently solves and exits 0. The causal chain (partial CSV -> free phantom stage -> mis-balanced confident schedule, warning lost on stdout) holds end-to-end. Proposed fix (raise / assert all stages in [1,num_stages] present before solving) is appropriate. Severity is genuine for the 03_run pipeline.

### [cuda-correctness] Tree CUDA kernels launch on the default stream while CudaManager creates and holds an unused per-dispatcher stream  _(M/medium, confirmed)_
- **evidence:** CudaManager (common/cuda/manager.cuh:17-27) creates `stream_` via cudaStreamCreate and exposes get_stream(), but NO kernel launch passes it: tree/cuda/dispatchers.cu:28,125,146,200,217 launch `<<<grid_dim, block_dim, shared_mem>>>` (3-arg, stream 0); cifar-*/cuda/all_kernels.cuh:45,88,111 launch `<<<blocks, TPB>>>` (stream 0). The cub calls (dispatchers.cu:54,60,86,96,164,173) also take no stream. get_stream() has no caller (grep shows only the declaration). So every dispatcher pays for a stream it never uses, and all GPU work serializes on the default stream regardless of which worker thread/chunk issued it.
- **claim:** The stream abstraction is dead/misleading, and default-stream-only execution defeats any intent to overlap concurrent CUDA chunks or overlap CUDA with the cub temp-alloc. More importantly it signals the per-stream attach path that the §1 note says is REQUIRED if CudaManagedResource is ever used is simply absent. For now correctness holds only because everything is the default stream + device-wide cudaDeviceSynchronize, but the design is a trap for whoever adds a second concurrent GPU chunk.
- **fix:** Either (a) thread `get_stream()` through every launch and the cub calls and replace the device-wide cudaDeviceSynchronize in dispatch_stage with cudaStreamSynchronize(stream_) — making the manager's stream real; or (b) delete stream_/get_stream() from CudaManager and document that everything is default-stream. Do not leave it half-wired.
- **verifier note:** All cited facts verified (paths actually under builtin-apps/ prefix, but content matches exactly). CudaManager (builtin-apps/common/cuda/manager.cuh:17,21,26) creates stream_ via cudaStreamCreate and exposes get_stream(), which has ZERO callers (grep confirms only the declaration line). Every kernel launch uses the default stream: tree/cuda/dispatchers.cu:28,125,146,200,217 use 3-arg <<<grid,block,shared>>>; cifar-dense and cifar-sparse all_kernels.cuh launches use <<<blocks,TPB>>>. The cub calls (DeviceRadixSort/DeviceSelect/DeviceScan at dispatchers.cu:54,60,86,96,164,173) take no stream arg. dispatch_stage (tree/cuda/dispatchers.cuh:45,58) synchronizes with device-wide cudaDeviceSynchronize, not cudaStreamSynchronize(stream_). CudaManagedResource (cu_mem_resource.cuh) has no per-stream attach path (no cudaStreamAttachMemAsync), confirming the §1-required attach is absent. The only stream_ consumer in the tree is an UNRELATED class in cu_bench_helper.cuh with its own constructor-injected stream member — not CudaManager::stream_ — which reinforces rather than weakens the finding. Characterization is correct and appropriately hedged: this is a dead/misleading-abstraction maintainability+correctness concern, not an active bug; correctness currently holds because everything serializes on the default stream under device-wide sync. The proposed fix (thread the stream through all launches+cub and switch to cudaStreamSynchronize, OR delete stream_/get_stream and document default-stream) is sound.

### [cuda-correctness] Hypothesis (1) confirmed: CudaManagedResource (cudaMallocManaged + cudaMemAttachHost, no stream-attach, default-stream launch) is genuinely broken on Tegra; the in-tree mitigation (switch callers to CudaPinnedResource) is sound  _(S/low, confirmed)_
- **evidence:** cu_mem_resource.cu:41 `cudaMallocManaged(&ptr, bytes, cudaMemAttachHost)` with nothing ever calling cudaStreamAttachMemAsync and all kernels on the default stream. On Tegra concurrentManagedAccess==0, a host-attached managed buffer is not guaranteed visible to the GPU without a stream attach, so kernel writes are partially/inconsistently visible to the later host read — exactly the run-to-run §1 symptom. The fix already shipped: both dispatcher.cuh files declare `::cuda::CudaManager<::cuda::CudaPinnedResource> mgr_` (tree dispatchers.cuh:62, cifar-dense dispatchers.cuh:64), and CudaPinnedResource (cu_mem_resource.cu:65-82) uses cudaHostAlloc(cudaHostAllocMapped)+cudaHostGetDevicePointer, i.e. zero-copy pinned which is physically coherent on the Jetson UMA and concurrently host-accessible.
- **claim:** Root cause and fix are correct. The residual risk is that CudaManagedResource is left in the tree as a working-looking class with only a code comment guarding it; a future caller could reintroduce the §1 race. The CudaManager `requires` clause still permits CudaManagedResource.
- **fix:** Keep the pinned mitigation. To prevent regression, either remove CudaManagedResource entirely (no caller) or tighten CudaManager's `requires` to CudaPinnedResource only, or add a `static_assert`/runtime check that refuses CudaManagedResource when cudaDevAttrConcurrentManagedAccess==0. Validate: differential tree+cifar CUDA tests green on Jetson AND a hybrid OMP-reads-while-CUDA-writes pipeline run (the case global-attach would have faulted).
- **verifier note:** Verified against real code. cu_mem_resource.cu:41 has cudaMallocManaged(&ptr, bytes, cudaMemAttachHost); no cudaStreamAttachMemAsync exists anywhere in the tree, and kernels run on the default/per-manager stream without per-buffer GPU attach — the §1 host-attach-only visibility defect on Tegra (concurrentManagedAccess==0) is genuine. The shipped mitigation is real and complete: tree/dispatchers.cuh:62, cifar-dense/dispatchers.cuh:64, AND cifar-sparse/dispatchers.cuh:60 all declare CudaManager<CudaPinnedResource>; CudaPinnedResource (cu_mem_resource.cu:65-91) uses cudaHostAlloc(cudaHostAllocMapped)+cudaHostGetDevicePointer = zero-copy mapped pinned, physically coherent on Jetson UMA and concurrently host-accessible. CudaManagedResource has zero remaining callers and is guarded only by the in-code §1 comment (cu_mem_resource.cu:29-40). The residual-regression claim is also accurate: CudaManager's requires clause (manager.cuh:13-14) still permits CudaManagedResource, so a future caller could reintroduce the race. Proposed fix (tighten requires to CudaPinnedResource only / remove the class / static_assert on concurrentManagedAccess) is sound and low-risk. Only nit: the finding notes cifar-dense+tree but the same mitigation also covers cifar-sparse; that strengthens, not weakens, the finding.

### [cuda-correctness] kSync=false plus error-free kernel launch wrappers means CUDA launch/exec errors are swallowed until the next device sync  _(S/low, confirmed)_
- **evidence:** Every dispatcher sets `constexpr bool kSync = false;` (tree dispatchers.cu:20, cifar-dense dispatchers.cu:7, cifar-sparse dispatchers.cu:7), so the per-stage `cudaGetLastError()/cudaDeviceSynchronize()` blocks are compiled out. The host launch wrappers (cifar-*/cuda/all_kernels.cuh conv2d_batch_cuda/maxpool/linear and tree raw `<<<>>>`) do NO cudaGetLastError after the launch. Error detection only happens at dispatch_stage/dispatch_multi_stage boundaries (dispatchers.cuh:44-45,57-58) which DO call cudaGetLastError+cudaDeviceSynchronize — but multi_stage batches several launches, so a bad launch in stage 4 is only reported after stage 6, mislabeling which stage failed. An invalid launch config (e.g. grid_dim==0 when n_brt_nodes==0, or too-large shared mem) returns cudaErrorInvalidConfiguration that is attributed to the wrong stage or, with the static-grid bug, never surfaces as wrong-size at all.
- **claim:** For a profiling harness whose entire value is trustworthy per-stage numbers, deferring and mis-attributing CUDA errors across a multi-stage chunk undermines the data. Combined with the static-grid bug, an undersized launch produces no error AND wrong output.
- **fix:** Add `CheckCuda(cudaGetLastError())` immediately after each `<<<>>>` launch in the host wrappers (cheap, no sync), keeping the heavy cudaDeviceSynchronize at chunk boundary. This catches launch-config errors at the right stage without per-stage sync overhead.
- **verifier note:** Core mechanism verified against the real code. kSync=false in all three dispatchers (tree dispatchers.cu:20, cifar-dense dispatchers.cu:7, cifar-sparse dispatchers.cu:7), so every per-stage `if constexpr (kSync){cudaGetLastError;cudaDeviceSynchronize}` block is compiled out. Host launch wrappers in cifar-*/cuda/all_kernels.cuh (conv2d_batch_cuda L45, maxpool2d_batch_cuda L88, linear_batch_cuda L111) and the raw tree `<<<>>>` launches (dispatchers.cu L28,125,146,200,217) do NO cudaGetLastError after launch. Error detection only happens at dispatch_stage (dispatchers.cuh:48-49) and dispatch_multi_stage (dispatchers.cuh:59-60), both of which DO call cudaGetLastError+cudaDeviceSynchronize. Crucially dispatch_multi_stage runs the whole start..end loop (L55-57) BEFORE its single boundary check, so a launch error in an early stage is only surfaced after the last stage in the chunk and is mis-attributed — confirmed. CheckCuda (helpers.cuh:16-24) does exit(1) on error, so errors are not silently lost forever, only deferred and mis-labeled by stage; for a per-stage profiling harness that defeats the data, so the severity framing is fair. The proposed fix (cheap CheckCuda(cudaGetLastError()) right after each launch, no sync) is correct and matches existing style. Two minor caveats, neither undermining the finding: (1) the 'grid_dim==0 when n_brt_nodes==0' example is technically wrong — a zero-size grid launches as cudaSuccess (no-op) in CUDA, it does not return cudaErrorInvalidConfiguration; the too-large-shared-mem example IS a genuine invalid-config case, so the point stands on that example. (2) The 'static-grid bug' is a separate referenced finding (SETUP_DEFAULT_LAUNCH_PARAMS at helpers.cuh:32-35 does declare grid_dim/block_dim as static), invoked here only as a compounding factor. Overall the finding is real and correctly characterized.

### [cuda-correctness] cudaDeviceSynchronize used at chunk boundaries serializes the whole device, conflicting with concurrent OMP/CUDA pipeline chunks and a shared global cub allocator  _(M/medium, confirmed)_
- **evidence:** dispatch_stage/dispatch_multi_stage end with device-wide `cudaDeviceSynchronize()` (dispatchers.cuh:45,58). tree cub stages additionally call cudaDeviceSynchronize mid-stage (dispatchers.cu:63,103,179) and use one process-global `cub::CachingDeviceAllocator g_allocator(true)` (dispatchers.cu:18) with cudaFree per call. In the pipeline (pipe/tree-cu/bm_gen_log.cu:91-132) CUDA and OMP chunks run as concurrent std::threads; the CUDA worker's repeated full-device sync + g_allocator alloc/free churn per item adds runtime overhead that lands directly in the profiled record_ ticks (logger.start_tick/end_tick wrap func, const.hpp worker_with_record).
- **claim:** Device-wide synchronization and per-item cub temp-storage alloc/free are runtime overheads attributed to the GPU chunk's measured time. Since the harness exists to measure runtime overhead, this systematically inflates/contaminates the CUDA chunk numbers vs a stream-scoped sync and a persistent temp buffer.
- **fix:** Use cudaStreamSynchronize on the manager stream instead of cudaDeviceSynchronize, and pre-allocate cub temp storage once per dispatcher (the commented-out `appdata.cuda_temp_storage` at dispatchers.cu:49,81 suggests this was intended) rather than DeviceAllocate/cudaFree every call. Validate timing deltas and that differential tests stay green.
- **verifier note:** All cited evidence verified exactly. dispatchers.cuh:45 and :58 — dispatch_stage and dispatch_multi_stage both end in CheckCuda(cudaDeviceSynchronize()) (device-wide, not stream-scoped). tree/cuda/dispatchers.cu:18 declares one process-global cub::CachingDeviceAllocator g_allocator(true); cub stages 2/3/6 call cudaDeviceSynchronize mid-stage at :63/:103/:179 and the commented-out appdata.cuda_temp_storage asserts sit at :49/:81, supporting the "intended persistent buffer" reading. pipe/tree-cu/bm_gen_log.cu:91-132 confirms CUDA and OMP chunks run as concurrent std::threads, and the CUDA dispatch lambda (disp.dispatch_multi_stage) is the body wrapped by worker_with_record's start_tick/end_tick, so the device sync + alloc/free overhead lands inside the profiled record_ ticks. Since the harness exists to measure runtime overhead, the contamination claim is sound.

Two refinements (do not falsify the finding): (1) The temp-storage churn is actually WORSE than stated — code allocates via g_allocator.DeviceAllocate but frees via raw cudaFree (:65/:111/:185) instead of g_allocator.DeviceFree, so the caching allocator's cache is never populated; every cub stage does a real cudaMalloc + real cudaFree per item, defeating the whole point of the caching allocator. (2) The proposed cudaStreamSynchronize(mgr_ stream) drop-in is not literally sufficient: tree kernels launch on the DEFAULT stream (no <<<...,stream_>>> argument) and cub calls take no stream, so a correct stream-scoped fix must also route all launches/cub through mgr_'s stream — i.e. effort is a bit larger than a one-line swap. Category is correctly performance/measurement-contamination, not a correctness bug.

### [cuda-correctness] Memory-resource do_allocate ignores the requested alignment and discards the real CUDA error on failure  _(S/low, confirmed)_
- **evidence:** Both CudaManagedResource::do_allocate (cu_mem_resource.cu:27) and CudaPinnedResource::do_allocate (cu_mem_resource.cu:65) take `std::size_t /*alignment*/` and ignore it; cudaMallocManaged/cudaHostAlloc only guarantee 256-byte alignment which happens to cover current types (glm::vec4=16B, float/uint32) but silently breaks any future over-aligned (>256B) pmr allocation. On failure both throw `std::bad_alloc()` after discarding the cudaError_t (e.g. line 42-44, 68-70, 74-76), so the actual reason (e.g. cudaErrorMemoryAllocation vs cudaErrorInvalidValue) is lost and the CUDA error state is left set, which a later CheckCuda may then misreport as a stale failure.
- **claim:** This is latent rather than active (no current type exceeds 256B alignment), but it is an unchecked assumption in a reusable allocator, and the dropped error string makes OOM diagnosis on the memory-constrained edge devices harder.
- **fix:** Assert/honor alignment (cudaMalloc* are over-aligned to 256; static_assert or check `alignment <= 256`), and log the cudaGetErrorString before throwing (or throw a message-carrying exception) so allocation failures are diagnosable. Pre-existing; mention-don't-rush.
- **verifier note:** Verified against /home/doremy/Desktop/ucsc/better-together/builtin-apps/common/cuda/cu_mem_resource.cu (the finding's "cu_mem_resource.cu" path; cited lines match exactly). CudaManagedResource::do_allocate (line 27) and CudaPinnedResource::do_allocate (line 65) both take `std::size_t /*alignment*/` and ignore it — confirmed. On failure both throw `std::bad_alloc()` discarding `cudaError_t err` (lines 42-44, 68-70, 74-76) — confirmed; the cudaGetErrorString is never logged, so OOM vs invalid-value is indistinguishable. The 256B over-alignment of cudaMallocManaged/cudaHostAlloc does cover all current backing types (resources back a pmr CudaManager used by tree/cifar dispatchers; types are glm::vec4=16B, float, uint32), so the alignment gap is latent, exactly as the finding states (it self-labels latent/pre-existing/low-risk/mention-don't-rush, confidence 0.75). One sub-claim is mildly overstated but well-hedged: 'the CUDA error state is left set, which a later CheckCuda may then misreport as a stale failure' — typical allocation errors (cudaErrorMemoryAllocation) are non-sticky and cleared by the returning call, so they would generally NOT poison a subsequent CheckCuda; the word 'may' keeps this from being wrong. Core observations are all factually correct; proposed fix (honor/assert alignment<=256, log cudaGetErrorString before throwing) is reasonable.

### [vulkan-engine] kiss-vk has essentially no teardown: vk::Device and vk::Instance are never destroyed, and there is no deviceWaitIdle before VMA allocator destruction (root cause of the Tegra exit segfault)  _(M/medium, overstated)_
- **evidence:** base_engine.cpp:44-48 ~BaseEngine() only calls vmaDestroyAllocator(g_vma_allocator); it never calls device_.destroy() or instance_.destroy() and never device_.waitIdle(). base_engine.hpp:42-45 instance_/device_/compute_queue_ are plain vk:: handles (not vk::raii), so they have no destructor and leak silently. grep over kiss-vk/*.cpp shows the ONLY explicit Vulkan-object destroy in the whole teardown path is sequence.cpp:31 destroyQueryPool. So at process exit the VMA allocator is destroyed and (via the private DynamicLoader dl_ member, base_engine.hpp:51) libvulkan is dlclose'd while the live vk::Device and every child object it owns are still alive but never explicitly torn down — the NVIDIA Tegra driver is left to reclaim them in an undefined order during static/atexit teardown. This matches bugs-found.md:297-339 exactly: bm-baseline-cifar-dense-vk and bm-gen-logs-*-vk print all results correctly, then segfault on exit (generalizes across VK binaries, only after the data is flushed), while OMP-only baselines never crash. Each dispatch stage fully fences (dispatchers.cpp:141-143 submit/wait_for_fence/reset_fence), so it is not in-flight GPU work — it is the missing ordered teardown.
- **claim:** Letting the driver clean up a never-destroyed device/instance at exit, after the VMA allocator is gone and libvulkan is being unloaded, is undefined and crashes on the Jetson driver. It also leaks the full device+instance every process, and makes the exit code non-zero, which already silently broke 03_run_schedule.py (had to switch to check=False).
- **fix:** Give BaseEngine a real ordered teardown: in ~BaseEngine, before vmaDestroyAllocator, call device_.waitIdle(); destroy the VMA allocator; then device_.destroy(); instance_.destroy(); and set g_vma_allocator=nullptr. Engine/Sequence/Algorithm children must be destroyed before this (they are, via member ordering) but they must actually destroy their handles (see other findings). Alternatively migrate the handles to vk::raii so destruction order is enforced by the type system. As an immediate band-aid the docs suggest disp.reset() before benchmark::Shutdown(), but the durable fix is the dtor.
- **verifier note:** The core defect is REAL and accurately read. In builtin-apps/common/kiss-vk/base_engine.cpp:44-48, ~BaseEngine() only calls vmaDestroyAllocator(g_vma_allocator) and never device_.destroy(), instance_.destroy(), or device_.waitIdle() (and never sets g_vma_allocator=nullptr). base_engine.hpp:42-45 confirms instance_/device_/physical_device_/compute_queue_ are plain vk:: handles (not vk::raii), so they leak with no destructor; dl_ (the DynamicLoader that owns/dlcloses libvulkan) is a member at base_engine.hpp:51. A grep over kiss-vk *.cpp/*.hpp confirms the ONLY explicit Vulkan destroy anywhere in teardown is sequence.cpp:31 destroyQueryPool (and Sequence::~Sequence even leaks its command_pool_ and fence_ — destroys only query_pool_). The dispatch fencing claim is correct: cifar-dense/vulkan/dispatchers.cpp:141-143 submit()/wait_for_fence()/reset_fence() per stage, so it is not in-flight GPU work. The bugs-found.md §9 evidence matches (exit segfault on bm-baseline-cifar-dense-vk / bm-gen-logs-*-vk after data flushed; OMP-only never crashes), and the check=False workaround is confirmed (scripts 03_run_schedule.py uses check=False with a comment citing bugs-found §9). The leak and missing ordered teardown / missing waitIdle are indisputable, and the proposed fix is sound.\n\nWHAT IS OVERSTATED: the title/claim assert this is THE \"root cause of the Tegra exit segfault\" (confidence 0.78). The repo's own bugs-found.md §9 explicitly labels the cause \"(unverified)\", \"Not yet reproduced under a debugger,\" and lists competing hypotheses (double-free after 1000 GPU iterations on Tegra; interaction with the managed-mem §1 path; static-destruction order of the engine vs google-benchmark globals). The proposed mechanism (VMA destroyed + libvulkan dlclose'd via dl_ while a live device/instance are reclaimed by the driver in undefined order) is plausible and self-consistent, but it is an inference, not a debugger-confirmed root cause. So: real defect, real leak, sound fix — but the causal certainty (\"root cause\") is asserted beyond what the evidence in the repo supports.

### [vulkan-engine] Sequence destructor leaks the command pool and fence (only the query pool is freed)  _(S/low, confirmed)_
- **evidence:** sequence.cpp:29-33 ~Sequence() destroys only query_pool_ (and only when timestamps are supported). It never calls device_ref_.destroyCommandPool(command_pool_) (created sequence.cpp:57) nor device_ref_.destroyFence(fence_) (created sequence.cpp:64). The class declares a protected destroy() (sequence.hpp:41) that is never defined or called.
- **claim:** Every Sequence leaks a VkCommandPool (and the command buffer it owns) and a VkFence. In the benchmarks one Sequence lives per Dispatcher so the absolute leak is small, but combined with the device never being destroyed these are live child objects at exit, contributing to the undefined teardown order behind the Tegra crash. The dead protected destroy() declaration signals the cleanup was intended but never written.
- **fix:** Implement Sequence::destroy() to destroyCommandPool(command_pool_), destroyFence(fence_), and (when present) destroyQueryPool(query_pool_); call it from ~Sequence(). Or hold these as vk::raii::CommandPool/Fence/QueryPool.
- **verifier note:** Verified against /home/doremy/Desktop/ucsc/better-together/builtin-apps/common/kiss-vk/sequence.cpp and sequence.hpp (the cited paths sequence.cpp/sequence.hpp resolve here). All core facts hold:

- ~Sequence() (sequence.cpp:29-33) destroys ONLY query_pool_, and only when timestamp_valid_bits_ != 0 && query_pool_ is set.
- command_pool_ is created at sequence.cpp:57 (createCommandPool) and is never destroyed; fence_ is created at sequence.cpp:64 (createFence) and is never destroyed. The owned command buffer (handle_, allocated at :76 from command_pool_) is also never explicitly freed — though it would be freed transitively if the pool were destroyed. So every Sequence leaks a VkCommandPool and a VkFence (and the command buffer with the pool).
- Members are raw vk:: handles, not vk::raii wrappers (sequence.hpp:54-59: vk::CommandBuffer, vk::CommandPool, vk::Fence, vk::QueryPool) — so there is no automatic cleanup; the leak is real.
- protected void destroy() is declared (sequence.hpp:41) but grep shows it is never defined or called anywhere — dead declaration, consistent with the finding that cleanup was intended but unwritten.
- Note on line numbers: the evidence cites sequence.cpp:57 for the command pool and :64 for the fence; these match exactly. The destructor span :29-33 also matches exactly.

Usage: Sequence is handed out via Engine::make_seq() as std::make_shared<Sequence> (engine.hpp:26-38); lifetime is shared_ptr, so the missing cleanup runs (or rather fails to run) at Sequence destruction — confirming the per-Sequence leak. No copy/double-free concern since the destructor frees neither pool nor fence.

Two caveats on the broader CLAIM (not the finding itself):
1. The 'one Sequence per Dispatcher, small absolute leak' and the 'device never destroyed → undefined teardown order behind the Tegra crash' parts are plausibility/context narrative, not separately proven here; the causal link to a Tegra crash is speculative. But this does not affect the correctness of the leak finding.
2. The leak is genuine but low-impact in practice (handful of long-lived Sequences); severity is modest. Proposed fix (implement destroy() to destroyCommandPool/destroyFence/destroyQueryPool and call from ~Sequence, or switch to vk::raii) is correct and appropriate.

Finding is real and correctly characterized as a resource leak (command pool + fence + command buffer) in ~Sequence with a dead protected destroy() declaration.

### [vulkan-engine] Algorithm destructor is =default and leaks every pipeline-related Vulkan object it creates  _(S/low, confirmed)_
- **evidence:** algorithm.hpp:25 ~Algorithm() = default; while the class owns shader_module_, pipeline_, pipeline_cache_, pipeline_layout_, descriptor_set_layout_, descriptor_pool_ (algorithm.hpp:105-110), all created in algorithm.cpp (createShaderModule:172, createPipelineLayout:291, createPipelineCache:293, createComputePipeline:308, createDescriptorSetLayout:208, createDescriptorPool:228). None are vk::raii and none are destroyed anywhere.
- **claim:** Each cached Algorithm permanently leaks 6 Vulkan objects (pipeline, cache, layout, descriptor-set-layout, descriptor-pool, shader-module). With a handful of cached algorithms per app the steady-state leak is bounded, but these are again live device children at process exit, feeding the undefined teardown ordering. A pipeline cache and pool leaking per algorithm is also a latent problem if the dispatcher were ever recreated in a long-running process.
- **fix:** Add a real ~Algorithm() that destroys all six handles via device_ref_ (guard on non-null), or convert the members to vk::raii types so they self-destruct in the correct reverse order.
- **verifier note:** Verified against the real code. algorithm.hpp:25 is `~Algorithm() = default;`. The class owns six plain (non-RAII) Vulkan handles as members (algorithm.hpp:105-110): shader_module_, pipeline_, pipeline_cache_, pipeline_layout_, descriptor_set_layout_, descriptor_pool_. vk.hpp includes plain <vulkan/vulkan.hpp> (NOT vulkan_raii.hpp), so vk::Pipeline/vk::ShaderModule/etc. are non-owning handle wrappers that do not self-destroy. All six are created via device_ref_.create*() in algorithm.cpp (createShaderModule:172, createDescriptorSetLayout:208, createDescriptorPool:228, createPipelineLayout:291, createPipelineCache:293, createComputePipeline:308). A repo-wide grep for destroyPipeline/destroyShaderModule/destroyDescriptorPool/destroyDescriptorSetLayout/destroyPipelineLayout/destroyPipelineCache/.destroy( in kiss-vk returns nothing — these handles are never destroyed anywhere. So each Algorithm leaks exactly those 6 objects; the count is correct (the allocated descriptor_sets_ are freed implicitly when the pool is destroyed... except the pool is itself leaked, so those leak too, but they are not separately counted, which is the right framing). The 'cached' characterization is accurate: apps hold Algorithms in `std::unordered_map<std::string, std::shared_ptr<kiss_vk::Algorithm>> cached_algorithms` (e.g. cifar-dense/vulkan/dispatchers.hpp:64), so the steady-state leak is bounded but the objects remain live device children at teardown, contributing to undefined destruction ordering. The proposed fixes (real ~Algorithm() with non-null-guarded device_ref_ destroys, or convert to vk::raii types) are both valid; effort S / risk low is reasonable. Finding is real and correctly characterized.

### [vulkan-engine] g_vma_allocator is a single process-global that is overwritten by each new Engine and never reset to nullptr on destroy  _(M/medium, confirmed)_
- **evidence:** base_engine.cpp:15 VmaAllocator g_vma_allocator = nullptr; (file-scope global). initialize_vma_allocator (base_engine.cpp:272) writes it unconditionally; ~BaseEngine (base_engine.cpp:45-47) destroys it but does NOT set it back to nullptr. vma_pmr.cpp:81/96/129/142/149 all use the bare global. Benchmarks construct Engines sequentially (e.g. bm_gen_log.cpp:15 then :79; bm_fully_vs_normal.cpp:39 then :116), and cifar dispatchers embed kiss_vk::Engine by value (dispatchers.hpp:62).
- **claim:** The design assumes exactly one Engine alive at a time. If two Engines ever coexist (two Dispatchers, a nested warmup engine, or a future multi-GPU/multi-app path) the second vmaCreateAllocator overwrites the first's global; the first Engine's pmr buffers then deallocate against the wrong/destroyed allocator -> use-after-free / double-free. Even in the current sequential usage, because the global is left dangling (not nulled) after ~BaseEngine, any stray VulkanMemoryResource deallocation after the engine dies calls vmaDestroyBuffer on a freed allocator. This is a fragile, easy-to-trip global.
- **fix:** Reset g_vma_allocator=nullptr in ~BaseEngine after vmaDestroyAllocator. Better: store the VmaAllocator as a member of BaseEngine and pass it into VulkanMemoryResource (it already receives a vk::Device) instead of using a file-scope global, so two engines cannot collide.
- **verifier note:** Every cited fact verified against the code. base_engine.cpp:15 declares `VmaAllocator g_vma_allocator = nullptr;` at file scope (extern in vma_pmr.hpp:15). initialize_vma_allocator() writes it unconditionally via vmaCreateAllocator (base_engine.cpp:272). ~BaseEngine (lines 44-48) calls vmaDestroyAllocator but does NOT reset the global to nullptr. vma_pmr.cpp uses the bare global at lines 81, 96, 129, 142, 149 (create/map/destroy/flush/invalidate). VulkanMemoryResource stores only device_ (engine.hpp:14 constructs it with device_ alone; member list confirms no VmaAllocator), so it always reaches for the global. cifar dispatchers embed kiss_vk::Engine by value (dispatchers.hpp:62). Benchmarks construct DispatcherT sequentially (bm_gen_log.cpp:15 and :79; bm_fully_vs_normal.cpp:39 and :116). The design is indeed a fragile singleton: two coexisting Engines would have the second vmaCreateAllocator overwrite the first's global, causing the first's pmr deallocations (vmaDestroyBuffer) to hit the wrong/destroyed allocator -> UAF/double-free. Fix (null on destroy; better, make the allocator a BaseEngine member passed into VulkanMemoryResource) is sound. Two fairness caveats lowering practical severity to latent rather than active: (1) the two DispatcherT instances live in separate, non-overlapping function scopes (each in its own static function, called sequentially from main: BM_pipe_warmup at line 186, schedule_auto at 238), so they never coexist today; (2) the 'stray deallocation after engine dies' variant cannot trigger under current ownership -- Engine owns the VulkanMemoryResource via unique_ptr (engine.hpp:76) and the dispatcher owns the Engine, and AppData buffers allocated via get_mr() are destroyed before each benchmark function returns while the engine/allocator is still alive. So the bug is a correctly-characterized latent/forward-looking robustness hazard, not a live defect in the current code paths.

### [vulkan-engine] Per-stage dispatch re-updates the descriptor set and re-records the whole command buffer, then does a full submit+fence-wait round trip, with only one descriptor set (num_sets(1))  _(L/medium, confirmed)_
- **evidence:** For each stage call the dispatcher does updateDescriptorSets (algorithm.cpp:88 via dispatchers.cpp:100-106), then cmd_begin/record_bind_core/record_bind_push/record_dispatch/cmd_end (dispatchers.cpp:132-139), then submit/wait_for_fence/reset_fence (dispatchers.cpp:141-143). Algorithms are built with ->num_sets(1) (dispatchers.cpp:83), so the single set 0 is rewritten on every invocation. cmd_begin uses eOneTimeSubmit (sequence.cpp:82-83), forcing a full re-record each time.
- **claim:** Every stage incurs descriptor re-write + command-buffer re-record + a synchronous CPU<->GPU fence round trip. This serializes the GPU pipeline (no overlap between stages) and is pure host-side overhead — exactly the runtime overhead this project profiles. Because the buffers are fixed per AppData, the descriptor set and command buffer could be recorded once and replayed. With num_sets(1) there is also no double-buffering, so even if submits were batched they could not overlap.
- **fix:** Record the command buffer once per (algorithm, AppData) and replay it; only update descriptors when bindings actually change (drop eOneTimeSubmit). For pipelined overlap, allocate num_sets>1 and rotate sets/command buffers so consecutive tasks don't serialize on a single fence. At minimum hoist update_descriptor_set out of the hot per-iteration path when bindings are invariant.
- **verifier note:** Core mechanics verified in the real code. algorithm.cpp:88 issues device.updateDescriptorSets on every update_descriptor_set call. In builtin-apps/cifar-sparse/vulkan/dispatchers.cpp every run_stage_N does update_descriptor_set + update_push_constant, then seq->cmd_begin / record_bind_core / record_bind_push / record_dispatch / cmd_end, then seq->submit / wait_for_fence / reset_fence (e.g. stage 1 lines 195-241; identical pattern through stage 9). All algos are built with ->num_sets(1) (lines 127,145,178), so the single set 0 is rewritten each call. sequence.cpp:82-83 confirms cmd_begin uses eOneTimeSubmit, and wait_for_fence (sequence.cpp:206) is a blocking waitForFences(UINT64_MAX). So each invocation genuinely incurs descriptor re-write + full command-buffer re-record + a synchronous CPU<->GPU fence round trip, all host-side runtime overhead. The benchmark (vulkan/bm_main.cpp:23-26) calls run_stage_1 in a tight timed loop, so this overhead is exactly what gets measured per iteration. Two corrections to the finding: (1) the cited line numbers (83, 100-106, 132-139, 141-143) do NOT match this file's layout (stages start at line 190+); they appear paraphrased/from a different version, though the named calls and num_sets(1)/eOneTimeSubmit facts are all real. (2) The 'serializes the GPU pipeline, no overlap between stages' claim is overstated for this app: the cifar-sparse stages are strictly data-dependent (each reads the prior stage's output buffer), so they must serialize regardless; the recoverable cost is the host-side re-record + fence round trip, not lost inter-stage GPU overlap. The double-buffering/num_sets>1 benefit applies to pipelining independent tasks (separate AppData), not these dependent intra-app stages. The proposed fix (record once per (algorithm, AppData) and replay, drop eOneTimeSubmit, hoist descriptor update when bindings are invariant) is valid since buffers are fixed per AppData.

### [vulkan-engine] HOST_CACHED non-coherent path flushes/invalidates ALL allocations on every submit and every fence-wait, O(allocations) per stage  _(M/medium, confirmed)_
- **evidence:** Sequence::submit calls mr_->flush_all() (sequence.cpp:189) and wait_for_fence calls mr_->invalidate_all() (sequence.cpp:215). flush_all/invalidate_all iterate the entire allocations_ map under the mutex and call vmaFlush/InvalidateAllocation per buffer (vma_pmr.cpp:139-151). The map holds every buffer ever allocated from the resource (weights + all stage I/O for the whole AppData, and in pooled runs make_dataset allocates kPoolSize AppData from the same mr).
- **claim:** Each of the ~9 (cifar) / 7 (tree) stages per task flushes and invalidates every buffer of every AppData allocated from that memory resource, not just the buffers the stage touches. With a pool of AppData sharing one mr this is O(total_buffers) cache maintenance per stage — and it holds the mutex, serializing against concurrent CPU-tier worker threads doing allocations. On the cached Mali heap this is the correctness fix from §7, but doing it whole-map per submit is far more work than needed and inflates the very GPU-stage wall-clock the profiler feeds to z3.
- **fix:** Track per-Sequence (or per-dispatch) the set of allocations actually bound this submit and flush/invalidate only those, rather than the entire allocations_ map. Skip the iteration entirely when the heap is coherent (detect once at allocator init and store a bool) instead of relying on VMA per-call no-ops while still walking the whole map and taking the mutex.
- **verifier note:** The core mechanism is real and verified in source. Sequence::submit() calls mr_->flush_all() (sequence.cpp:189) and wait_for_fence() calls mr_->invalidate_all() (sequence.cpp:215). Both flush_all/invalidate_all (vma_pmr.cpp:139-151) lock mutex_ and iterate the ENTIRE allocations_ map, calling vmaFlush/InvalidateAllocation(0, VK_WHOLE_SIZE) per buffer — not just the buffers the stage bound. There is exactly one VulkanMemoryResource per Engine (engine.hpp:14,76, get_mr() returns mr_ptr_.get()), so all weights and all AppData I/O buffers share that single map. Per-task this runs once per stage: cifar-sparse dispatchers.cpp has 9 submit() sites and tree dispatchers.cpp has 7, each followed by wait_for_fence() (e.g. dispatchers.cpp:239-240), so ~9/~7 full-map flush+invalidate sweeps per task — matching the claim. The proposed fixes (track per-submit bound allocations; detect coherent heap once and short-circuit the whole walk+mutex) are valid: even on coherent memory where vmaFlush/Invalidate are internal no-ops (per the comment at vma_pmr.cpp:136-138), the code still walks the whole map and takes the lock every submit/wait. The concurrency angle has basis too: dispatchers include pipeline/task.hpp and recent commits added per-PU disjoint AppData slices, so multiple worker threads can contend on mutex_. Two minor mischaracterizations that do not undermine the finding: (1) the evidence cites 'kPoolSize AppData from make_dataset' — kPoolSize (appdata.hpp:19 = 2) is actually a conv pooling-window kernel parameter, not a dataset pool count, and I did not find a make_dataset symbol; the real basis for 'many AppData per mr' is the shared single mr plus the multi-threaded pipeline, not kPoolSize. (2) It is a performance/overhead finding, not a correctness bug — the whole-map maintenance is conservatively correct (it is the §7 cache-maintenance fix), just more work than necessary. Net: confirmed as a real, correctly-characterized performance finding.

### [pipeline-runtime] No schedule-vs-device PU validation: a CPU tier / GPU backend the device lacks crashes via std::terminate inside a worker thread  _(M/low, confirmed)_
- **evidence:** validate_schedule_coverage (builtin-apps/pipeline/schedule.hpp:122-155) only checks stage *coverage* [1,n], never whether the referenced PU exists on the device. In pipe/cifar-dense-vk/bm_gen_log.cpp:116-127 an OMP chunk does get_processor_type_from_chunk_config(...) then get_cores_by_type(cpu_pt). config_reader.hpp:88-96 happily maps "Little"->kLittleCore regardless of device. On the Big-only MiniPC get_cores_by_type(kLittleCore) (app.hpp) returns an EMPTY vector, so dispatch_multi_stage(cores, cores.size()=0, ...) runs with num_threads(0) and bind_thread_to_cores(empty) calls sched_setaffinity with an empty cpuset -> EINVAL -> throw std::runtime_error (affinity.hpp:83-99). That throw happens inside the std::thread body (worker_with_record, const.hpp:80-110) with no surrounding try/catch -> std::terminate, hard abort. Same gap for a Vulkan/CUDA chunk dispatched on a device whose binary/backend isn't present.
- **claim:** A single mismatched schedule (wrong device_id, or a z3 solution generated for a different SoC topology) takes down the whole run with an uncatchable terminate instead of a skip+warn. This is exactly the 'crashes hard on bad input' failure mode; coverage validation gives a false sense of safety because it passes these schedules.
- **fix:** Add a validate_schedule_devices(schedule) step alongside validate_schedule_coverage in main(): for each chunk, reject (or skip-with-warn) OMP chunks whose tier has empty get_cores_by_type, and GPU chunks whose backend != this binary's backend. Either skip the offending schedule (continue) and log, or fail that one schedule without aborting the batch. Also wrap the per-chunk lambda bodies' dispatch in the worker so an exception is captured into the logger/return rather than terminating.
- **verifier note:** The finding is real and the crash chain holds on close reading. Verified each link:

1. validate_schedule_coverage (builtin-apps/pipeline/schedule.hpp:122-155) only checks contiguous stage coverage [1,n]; it never inspects the chunk's PU/tier or backend. Confirmed.

2. config_reader.hpp:88-96 maps "Little"/"Medium"/"Big" -> ProcessorType unconditionally, with no device cross-check. Confirmed.

3. bm_gen_log.cpp:115-127: an OMP chunk calls get_processor_type_from_chunk_config then, inside the worker lambda, get_cores_by_type(cpu_pt) and dispatch_multi_stage(cores, cores.size(), ...). Confirmed verbatim.

4. The empty-vector outcome is real but via a slightly different mechanism than the finding states: get_cores_by_type (app.hpp:34-47) returns a REFERENCE to a global (g_lit_cores/g_med_cores/g_big_cores), not the throwing default branch. Those globals (app.hpp:15-17, populated in app.cpp:63-65 only from cores the device actually has) start empty, so on a Big-only device get_cores_by_type(kLittleCore) yields an empty vector and cores.size()==0. Net effect matches the finding.

5. dispatch_multi_stage (cifar-dense/omp/dispatchers.hpp:68-83) does `#pragma omp parallel num_threads(0)` then calls bind_thread_to_cores(empty). bind_thread_to_cores (affinity.hpp:33-101) has NO empty-set guard; on Linux it builds an empty cpuset and calls sched_setaffinity, which fails (EINVAL) and throws std::runtime_error (lines 83-99, with the human-readable "<empty>" message — itself evidence the empty case was anticipated but not prevented).

6. The throw escapes the std::thread body (worker_with_record, const.hpp:80-111; func(app) at line 99 is unguarded). main()'s try/catch blocks (bm_gen_log.cpp:197-204, 208-213) cover only JSON parse and coverage validation, not pipeline execution — and crucially could not catch a worker-thread exception anyway, since an exception leaving a std::thread's top-level function calls std::terminate. Hard abort confirmed; coverage validation does give false safety.

Two small caveats that do not change the verdict: (a) the empty-vector path is a reference to an empty global, not the throwing default branch as the wording loosely implies; (b) num_threads(0) is non-conforming OpenMP and runtime-dependent (libgomp/LLVM typically clamp to 1 thread), but the terminate outcome holds either way because the throw escapes the thread body. The same gap applies to GPU chunks dispatched on a device whose backend isn't present, and the same code shape exists across the other pipe/* bm_gen_log.cpp variants. The proposed fix (a validate_schedule_devices step plus per-worker exception capture) is appropriate.

### [pipeline-runtime] Worker threads throw std::runtime_error directly (nullptr token, 'wrong backend in mode', Logger OOB) with no per-thread catch -> recursive terminate  _(M/low, confirmed)_
- **evidence:** const.hpp:62-64 and :93-95 'if (app == nullptr) throw std::runtime_error("App is nullptr")' execute on the worker thread. Logger::start_tick/end_tick (record.hpp:79-91) throw std::out_of_range if chunk_id>=kMaxChunks, also on the worker thread. bm_gen_log.cpp:62 throws std::invalid_argument('Don't run Vulkan in CUDA mode') from inside BM_pipe_warmup's thread-spawning loop body region. None of the std::thread bodies are wrapped in try/catch, so any of these escapes the thread and triggers std::terminate. A nullptr token is in fact reachable: if the schedule has more chunks than tokens can keep all queues fed, or the queue is mis-sized, dequeue() never returns a null (it spins), but the explicit nullptr check exists precisely because the author feared it -- and if it ever fires it aborts instead of degrading.
- **claim:** The executor's error strategy is 'throw on the worker thread', which is the worst place to throw because it is unrecoverable. Robustness hypothesis #2's 'worker nullptr-throw' is confirmed as a real terminate vector, not merely cosmetic.
- **fix:** Wrap each worker body in try/catch that records the failure (set an atomic error flag / promise) and returns, so the main thread can join, report, and move to the next schedule. Replace the nullptr throw with an assert or a logged early-return.
- **verifier note:** Core claim is real and correctly characterized. Verified in /home/doremy/Desktop/ucsc/better-together: worker() and worker_with_record() are defined in pipe/cifar-dense-vk/const.hpp (the cited 62-64 and 93-95 are exactly the `if (app == nullptr) throw std::runtime_error("App is nullptr")` lines). Logger::start_tick/end_tick at builtin-apps/pipeline/record.hpp:79-91 throw std::out_of_range when chunk_id>=kMaxChunks(16). In bm_gen_log.cpp these functions are launched via threads.emplace_back(worker/worker_with_record, ...) (lines 40-60, 103-130) with NO try/catch around the bodies and no catch around join() — so any throw escaping a worker thread calls std::terminate. Confirmed as a real terminate vector, not cosmetic.

Reachability nuance is accurate as the finding itself states: spsc_queue.hpp dequeue() returns false when empty (worker spins) and only yields an item that was actually enqueued; bm_gen_log only enqueues valid dataset pointers, so the nullptr throw is effectively dead/defensive and not a live vector. The genuinely reachable worker-thread throw is the Logger OOB (a machine-generated schedule with >16 chunks, or chunk_id>=kMaxChunks, hits worker_with_record's start_tick/end_tick on the worker thread).

Two minor mischaracterizations, neither undermining the finding: (1) bm_gen_log.cpp:62 actually throws std::invalid_argument("Don't run CUDA in Vulkan mode") — the finding inverts the message to "Don't run Vulkan in CUDA mode"; and this throw executes on the MAIN thread inside the spawn loop (still uncaught -> terminate via BM_pipe_warmup at main line 186), not on a worker thread, so grouping it under "worker nullptr-throw" is loose. (2) The finding double-attributes the nullptr throw to both const.hpp and record.hpp; it lives in const.hpp's worker functions (record.hpp only holds the Logger throws). Proposed fix (wrap worker bodies in try/catch, set error flag, downgrade nullptr throw to assert/early-return) is reasonable.

### [pipeline-runtime] Pipeline token pool silently off-by-one: 16 tokens enqueued into a capacity-15 SPSC queue, the 16th enqueue's false return is ignored  _(S/low, confirmed)_
- **evidence:** SPSCQueue reserves one slot: with Size=16, full() triggers when next_head==tail, so usable capacity is 15 (spsc_queue.hpp:20-31,62-65). bm_gen_log.cpp:20-21 and :84-85 do `for (i=0; i<kPoolSize(=16); ++i) queues[0].enqueue(dataset[i].get());` and DISCARD the bool. The 16th enqueue returns false; that AppData* is dropped from circulation. make_dataset also allocated 16 AppData but only 15 ever flow.
- **claim:** The pool is one token smaller than the author intended, and the ignored return value hides the Size-1 capacity invariant. Not a deadlock (15 tokens still circulate the ring), but it means 'kPoolSize=16' is a lie, wastes one allocation, and is a latent foot-gun: anyone who later sets kPoolSize to fill the queue exactly will silently lose a token and possibly under-feed a long pipeline.
- **fix:** Either size the queue as SPSCQueue<T, kPoolSize*2> (or kPoolSize+1 rounded to pow2) so all kPoolSize tokens fit, or loop with `if (!queues[0].enqueue(...)) break;` and assert the count. Document that SPSC usable capacity is Size-1.
- **verifier note:** Verified real and correctly characterized. In /home/doremy/Desktop/ucsc/better-together/pipe/cifar-dense-vk/const.hpp: kPoolSize=16 (line 18) and QueueT=SPSCQueue<AppDataT*, kPoolSize> i.e. Size=16 (line 21). The SPSCQueue (builtin-apps/pipeline/spsc_queue.hpp:20-31,62-65) reserves one slot — enqueue returns false when next_head==(head+1)&mask equals tail, so usable capacity is Size-1=15. In bm_gen_log.cpp the preload loop `for (i=0; i<kPoolSize; ++i) queues[0].enqueue(dataset[i].get());` (lines 20-22 in BM_pipe_warmup and 84-86 in BM_pipe_cifar_dense_vk_schedule_auto) discards the bool. Ring math from empty (head=tail=0): the first 15 enqueues succeed (head advances 0->15), the 16th (i=15) computes next_head=(15+1)&15=0==tail=0 and returns false, so dataset[15] is silently dropped. make_dataset(disp, kPoolSize) allocates 16 AppData but only 15 ever circulate. Correctly NOT a deadlock — 15 tokens still flow through the ring of per-chunk queues (next = (chunk_id+1)%n_chunks), which is sufficient (>=1) to keep the pipeline alive. So 'kPoolSize=16' is effectively 15: one wasted allocation and a latent off-by-one foot-gun masked by the ignored return value, exactly as claimed. The same pattern is duplicated in the sibling pipe/cifar-sparse-vk/bm_gen_log.cpp and pipe/tree-vk/bm_gen_log.cpp (lines 21 and 85 in each). One precision note: the cited path 'bm_gen_log.cpp' resolves to the pipe/*-vk/ copies, not builtin-apps/cifar-dense/vulkan/bm_gen_log.cpp (a different file whose enqueues are hidden behind macros). Proposed fix (size queue as kPoolSize+1->pow2, or break+assert on false, plus document Size-1 capacity) is appropriate.

### [pipeline-runtime] x86 TSC frequency calibration blocks 10ms and assumes constant/invariant TSC; first stats call pays a hidden 10ms and miscalibrates under frequency scaling  _(M/medium, overstated)_
- **evidence:** record.hpp:41-55 get_counter_frequency() on x86 sleeps 10ms via this_thread::sleep_for inside a static lambda, dividing (c1-c0) by elapsed seconds. This is the band-aid for the old freq=0->inf bug (commit 22b1ac2). But: (a) it runs once, lazily, on the first cycles->ms conversion -- on the timing-critical path that first call stalls 10ms; (b) a 10ms window is noisy and, on a CPU without invariant TSC or under DVFS, the rdtsc rate != steady_clock rate during measured kernels, so every duration_ms feeding the parser is systematically biased by however much the core boosted/throttled between calibration and measurement. There is no warmup/averaging and no check that the TSC is invariant.
- **claim:** The B2 timeline numbers on x86 (rocky-ryzen/MiniPC, the easiest Vulkan target) are converted with a one-shot 10ms calibration that can be off by the boost ratio, silently skewing the ms values that 04_parse_schedules.py and the optimizer consume. The inf/nan was fixed but replaced with a quieter accuracy bug.
- **fix:** Calibrate over a longer window (>=100ms) averaged a few times, or read the TSC frequency from CPUID 0x15/0x16 / /proc/cpuinfo where available; pin the calibration thread; verify constant_tsc/nonstop_tsc and warn if absent. Better: on x86 measure with clock_gettime(CLOCK_MONOTONIC) directly instead of rdtsc so no frequency conversion is needed.
- **verifier note:** Code confirmed but the file path in the finding is wrong: it is builtin-apps/pipeline/record.hpp, not utility/include/record.hpp. Lines 41-55 match exactly: x86 get_counter_frequency() is a Meyers-style lazy static lambda that does sleep_for(10ms), reads rdtsc before/after, and divides by steady_clock seconds — the documented band-aid for the old freq=0->inf bug.

Two of the finding's central claims are overstated/false:

(1) "First call stalls 10ms on the timing-critical path" — FALSE. get_counter_frequency() is only invoked from reporting/conversion methods (dump_records, dump_records_metadata, dump_records_for_python, print_chunk_stats, cycles_to_ms). The measurement hot path — now_cycles()/start_tick()/end_tick() (lines 79-91) — never calls it. A repo-wide grep confirms no other caller (only an unrelated copy in utility/query_cpuinfo.cpp). So the 10ms is paid once during post-run stats/dump, not during any measured kernel window.

(2) "Miscalibrates under DVFS / off by the boost ratio" — largely WRONG. On invariant-TSC x86 (constant_tsc/nonstop_tsc — present on essentially all CPUs since ~2010, including the rocky-ryzen/MiniPC targets) rdtsc ticks at a FIXED rate decoupled from core frequency/boost. Calibrating that fixed rate against a steady clock is the standard correct technique, not a source of boost-ratio bias. The systematic skew the finding describes only occurs on non-invariant-TSC CPUs, which these targets are not.

What survives, and is the only real residual issue: a single 10ms calibration has some jitter, with no averaging/warmup and no invariant-TSC (constant_tsc/nonstop_tsc) sanity check — a minor robustness gap, not a silent systematic accuracy bug. The data-flow premise (ms values flow to 04_parse_schedules.py/optimizer) checks out: scripts/collect/04_parse_schedules.py and 05_timeline.py do consume these dumps. Net: a real but minor robustness nit dressed up as a correctness bug with a mischaracterized hot-path cost and an incorrect DVFS/boost-ratio mechanism.

### [pipeline-runtime] Logger timeline drops any record where end<=start (sub-resolution kernels) and print_chunk_stats has unguarded CV/percentile math  _(S/low, overstated)_
- **evidence:** dump_records_for_python (record.hpp:131) and print_chunk_stats (record.hpp:152) skip a record unless `rec.end > rec.start`. A genuinely fast stage where two consecutive rdtsc/cntvct reads land in the same tick (end==start) is silently dropped, not recorded as ~0. Separately print_chunk_stats computes cv = std_dev/avg_time (record.hpp:205) with no avg_time>0 guard, and percentile indices `static_cast<int>(0.9*size)` (record.hpp:208-210) with no clamp -- safe for size>=2 but UB/garbage for size==1 vs the newer bt_prof percentile() which clamps. These are the same class of silent defect the freq/4-chunk commit fixed, left unfixed in the stats reader.
- **claim:** The B2 stats path still has silent guards missing: fast stages vanish from the timeline (under-counting that biases per-stage duration upward in aggregate), and the legacy print_chunk_stats CV/percentile can divide by zero / index oddly. The newer pipe/bm_prof_common.hpp got the guards (mean>0, clamped percentile) but record.hpp::Logger did not.
- **fix:** Use end>=start (record zero-duration as a real 0 or a 1-cycle floor) rather than end>start; guard cv with avg_time>0 ? : 0.0; reuse the clamped nearest-rank percentile from bm_prof_common instead of the raw static_cast index.
- **verifier note:** Mixed finding, verified against /home/doremy/Desktop/ucsc/better-together/builtin-apps/pipeline/record.hpp (lines 131, 152, 205, 208-210) and /home/doremy/Desktop/ucsc/better-together/pipe/bm_prof_common.hpp (126-132, 205).

REAL part: The `end > start` filter at record.hpp:131 (dump_records_for_python) and :152 (print_chunk_stats) does silently drop any record where end==start. A sub-resolution kernel whose two counter reads land on the same tick is dropped rather than recorded as ~0, which biases aggregate per-stage durations upward by losing the fastest samples. Correctly characterized; proposed `end >= start` fix is appropriate. The contrast with bm_prof_common.hpp is also real: it has `mean > 0 ? stddev/mean : 0.0` (line 205) and a clamped nearest-rank percentile() (126-132); record.hpp::print_chunk_stats has neither guard.

OVERSTATED/FALSE parts: (1) The cv = std_dev/avg_time divide-by-zero is NOT reachable. avg_time is computed only after the durations_ms.empty() early-return (line 159), and every pushed duration came from `end > start` so is strictly positive => total_time>0 => avg_time>0. The missing guard is defensive-only, no actual DBZ. (2) The percentile "UB/garbage for size==1" claim is false. For size==1, static_cast<int>(0.9*1)=static_cast<int>(0.99*1)=0, a valid index returning the sole element. For all sizes 0.9/0.95/0.99*size < size, so indices are always in-bounds; no UB, no out-of-bounds, no clamp needed for correctness. The evidence text even concedes "safe for size>=2" then wrongly flags size==1.

Net: one genuine silent defect (end==start drop), but the two "unguarded math => DBZ / UB / garbage" claims cannot actually trigger given the upstream end>start filter and the bounded percentile multipliers.

### [pipeline-runtime] Hardcoded warmup test_schedule + per-device hardcoded schedule tables make the executor brittle and device-coupled  _(M/low, confirmed)_
- **evidence:** Every bm_gen_log main() hardcodes a 2-chunk test_schedule {OMP stages1-3, GPU stages4-6} (bm_gen_log.cpp:167-184) that does NOT cover all kNumStages and is never run through validate_schedule_coverage -- it only works because BM_pipe_warmup processes exactly the enqueued tokens in a 2-chunk ring. first_present_cpu_type() (app.hpp) was added to stop the OMP warmup tier from throwing on a device lacking Little, which confirms the fragility is known. Worse, the OLD variant builtin-apps/cifar-dense/vulkan/bm_gen_log.cpp still ships 10 fully hardcoded per-schedule functions gated on `if (g_device_id != "3A021JEHN02756") return 0;` (lines 374-412) -- a parallel, device-pinned, now-dead executor that includes a nonexistent "common.hpp" and won't compile, yet sits next to the live JSON-driven pipe/ executors.
- **claim:** There are two divergent executor implementations (the live JSON-driven pipe/* and the dead device-pinned builtin-apps/*/vulkan/bm_gen_log.cpp). The warmup schedule is a magic constant that bypasses validation and silently assumes >=6 stages and a present GPU backend. This is brittle and confusing; the dead file is a maintenance trap.
- **fix:** Delete (or clearly retire) the device-pinned builtin-apps/*/vulkan/bm_gen_log.cpp variants since pipe/* superseded them. Derive the warmup schedule from the actual loaded schedule (e.g. first valid one) or from a validated minimal cover, and run it through validate_schedule_coverage + the proposed device-PU check so warmup can't reference an absent backend.
- **verifier note:** All factual claims verified against the real code.

(1) The hardcoded 2-chunk warmup test_schedule {OMP stages 1-3, Vulkan stages 4-6, cpu_proc_type = first_present_cpu_type()} exists at pipe/cifar-dense-vk/bm_gen_log.cpp:167-184 and identically in pipe/cifar-sparse-vk/bm_gen_log.cpp:167 and pipe/tree-vk/bm_gen_log.cpp:182. It is passed to BM_pipe_warmup and is NOT run through validate_schedule_coverage; that check (validate_schedule_coverage(schedules[vi], kNumStages)) is applied only to JSON-loaded schedules (line ~209). kNumStages is 9 (pipe/cifar-dense-vk/const.hpp:11), so the warmup deliberately under-covers (stages 1-6) and assumes a present Vulkan backend. Confirmed.

(2) first_present_cpu_type() lives in builtin-apps/app.hpp:28 and its own comment states it exists so 'a hardcoded tier (e.g. Little) never throws on a device that lacks it (the Big-only MiniPC has no Little cores)' -- directly confirming the fragility the finding describes.

(3) The old builtin-apps/cifar-dense/vulkan/bm_gen_log.cpp ships 10 fully hardcoded per-schedule functions in namespace device_3A021JEHN02756, gated in main() by `if (g_device_id != \"3A021JEHN02756\") return 0;` (lines 374-376). It #includes \"common.hpp\" (line 4), which does not exist anywhere in the source tree (only glm's unrelated common.hpp under build/_deps), and it is NOT referenced by any CMake target -- CMakeLists.txt only builds the pipe/*/bm_gen_log.cpp executors (lines 270-272). So it is genuinely dead and would not compile. Confirmed as a maintenance trap / divergent dead executor.

Caveats: this is a maintainability finding, not a correctness bug -- the warmup intentionally only primes the two execution paths and doesn't need full-stage coverage, so 'brittle' is mildly editorialized. Also the dead file's functions are named cifar_sparse while sitting in the cifar-dense directory, which is extra evidence of staleness rather than a misstatement. Effort M / risk low / proposed fix (delete dead variant; derive or validate the warmup schedule) are all reasonable.

### [python-collection] --minimize_mode tmax is silently ignored; tmax and gapness schedule JSONs are identical  _(S/low, confirmed)_
- **evidence:** 02_gen_schedule_merged.py:164 calls solve_optimization_problem(stage_timings, args.num_solutions, app) with NO minimize_mode. solver.py:21 `def solve_optimization_problem(stage_timings, num_solutions=30, app_name=None)` has no minimize_mode param, and at solver.py:39 calls add_chunk_time_constraint(opt, x, core_types, num_stages, stage_timings_data) — leaving constraints.py:28 default minimize_mode="gapness". So opt.minimize(Gapness) (constraints.py:73) is ALWAYS used. args.minimize_mode (02:115) is consumed ONLY for the output filename (02:151, schedules_{table}_{mode}.json). Result: schedules_btpm_tmax.json and schedules_btpm_gapness.json have byte-identical solution content; the tmax objective the user selected on Jetson/MiniPC/Samsung was never actually optimized.
- **claim:** The whole tmax-vs-gapness comparison the pipeline is built to produce is bogus: both modes solve gapness. Any paper claim that 'tmax minimizes makespan' is unsupported by what the code ran. This is the single highest-value defect in the area.
- **fix:** Thread minimize_mode end-to-end: add minimize_mode param to solve_optimization_problem and pass it into add_chunk_time_constraint; in 02 pass args.minimize_mode through. Also note constraints.py:74 checks for literal "max_time" while the CLI/value is "tmax" — normalize the token (map tmax->max_time or change the constraint branch) so the else-branch ValueError doesn't fire once wired up.
- **verifier note:** Verified against the actual code; every cited line matches. solve_optimization_problem (solver.py:21) has signature (stage_timings, num_solutions=30, app_name=None) with NO minimize_mode param. 02_gen_schedule_merged.py:164 calls it as solve_optimization_problem(stage_timings, args.num_solutions, app) — no mode passed. solver.py:39-41 calls add_chunk_time_constraint(opt, x, core_types, num_stages, stage_timings_data) with no mode, so constraints.py:28 default minimize_mode="gapness" applies, making constraints.py:72-73 always execute opt.minimize(Gapness). args.minimize_mode (02:115, choices ["gapness","tmax"]) is consumed ONLY for the output filename (02:151, f"schedules_{table_type}_{minimize_mode}.json"). Confirmed: both tmax and gapness runs optimize gapness, so the two JSONs differ only by filename, not solution content — the tmax-vs-gapness comparison is bogus as run. The secondary note is also correct and load-bearing: constraints.py:74 branches on literal "max_time" while the CLI value is "tmax" (02:64), so naively threading the raw token would hit the else-branch ValueError (constraints.py:77) — normalization (tmax->max_time) is required as part of any fix. One minor scoping caveat for the parent: this defect lives in the CPU/GPU pipeline; the sibling gen_schedule_tpu.py hardcodes minimize_mode="gapness" (line 109) and also never threads it, but that path is out of the cited evidence's scope. Finding is real, correctly characterized, fix and token-mismatch caveat are accurate.

### [python-collection] minimize_mode token mismatch: CLI emits 'tmax' but constraints.py only accepts 'max_time' (would raise once wired)  _(S/low, confirmed)_
- **evidence:** 02_gen_schedule_merged.py:54-67 and 03_run_schedule.py:90 use choices=["gapness","tmax"]. constraints.py:72-77 branches on minimize_mode=="gapness" / elif =="max_time" / else raise ValueError(f"Invalid minimize mode: {minimize_mode}"). 'tmax' matches neither, so the moment anyone fixes the threading bug above by passing args.minimize_mode straight through, every tmax run dies with ValueError.
- **claim:** Latent landmine that turns the obvious fix for the previous finding into a crash; the two bugs mask each other today (mode never reaches the constraint, so the bad token never trips).
- **fix:** Pick one vocabulary. Simplest: in constraints.py accept "tmax" (or map at the 02 boundary: mode = {"tmax":"max_time"}.get(args.minimize_mode, args.minimize_mode)). Add a unit test asserting tmax produces a different T_max objective than gapness on a skewed timing table.
- **verifier note:** Verified against the real code. CLI args define choices=["gapness","tmax"] in both 02_gen_schedule_merged.py:64 and 03_run_schedule.py:90. constraints.py add_chunk_time_constraint (lines 72-77) branches on minimize_mode=="gapness" / elif=="max_time" / else raise ValueError(f"Invalid minimize mode: {minimize_mode}"). The token "tmax" matches neither "gapness" nor "max_time", so wiring it through would raise ValueError. The masking claim is also correct: today minimize_mode never reaches the constraint. In 02, args.minimize_mode (line 115) is only used for stdout and the output filename (line 151, schedules_{table_type}_{minimize_mode}.json); the solver is called as solve_optimization_problem(stage_timings, args.num_solutions, app) with no mode (solver.py:21 signature has no minimize_mode param), and solver.py:39-41 calls add_chunk_time_constraint(opt, x, core_types, num_stages, stage_timings_data) without minimize_mode, so it defaults to "gapness". Thus the vocabulary mismatch is a latent landmine that activates the moment someone threads args.minimize_mode through to the constraint. Effort S / risk low is fair. One minor note: the proposed fix's mapping {"tmax":"max_time"} is the cleanest since the constraint's internal Z3 variable is named T_max and the metric key is "max_time" (solver.py:62), so aligning on "max_time" internally is consistent.

### [python-collection] 00_bm.py is a dead path: invokes retired `xmake r`, writes the same CSVs the new loader/export produce  _(S/low, confirmed)_
- **evidence:** 00_bm.py:180 `command_template = f"xmake r {target} ..."` with target `bm-fully-{app}-{backend}` (00_bm.py:167). xmake was retired 2026-06-16 (CLAUDE.md), no xmake.lua remains, so the run path can never succeed. Its parse_benchmark_data (00_bm.py:51-79) scrapes PYTHON_DATA_START/NORMAL/FULLY markers into pandas and writes isolated.csv/btpm.csv (00_bm.py:198-201) — exactly the columns export_btpm_csv.py:51 now regenerates from the schema-validated JSONL store. scripts/collect/README.md:6 itself labels 00_bm.py legacy. The --only-aggregate branch (re-parse existing .log files) is the only still-runnable part.
- **claim:** Confirms hypothesis (1): 00_bm.py's run path is dead and its parsing duplicates the new profiling_loader/export_btpm_csv path (two independent implementations of 'benchmark text -> stage CSV'). Keeping it invites someone to run a no-op or to 'fix' the wrong parser.
- **fix:** Per repo policy (leave pre-existing dead code, mention don't delete): add a top-of-file banner that the run path is retired and point to bm-prof + export_btpm_csv.py; if the --only-aggregate path is still wanted for old logs, split it out. Don't silently keep an `xmake r` invocation that 02's CSV consumer depends on.
- **verifier note:** All cited facts verified against /home/doremy/Desktop/ucsc/better-together/scripts/collect/00_bm.py. Line 180 is `command_template = f"xmake r {target} --device {device} -l off -p -t 1"` with target = `bm-fully-{app}-{backend}` (line 167). `find . -name xmake.lua` returns nothing and CLAUDE.md states xmake was retired 2026-06-16, so the run path (lines 178-206) can never succeed — it raises RuntimeError on the nonzero exit. parse_benchmark_data (lines 51-79) does scrape `### PYTHON_DATA_START ###` / `# NORMAL_BENCHMARK_DATA` / `# FULLY_BENCHMARK_DATA` into pandas, and the run path writes isolated.csv (line 198) + btpm.csv (line 199). export_btpm_csv.py independently regenerates those same two files (line 29: SCEN_OUT={"isolated":"isolated.csv","interference":"btpm.csv"}) for the same z3 consumer (its docstring lines 4-15 say exactly this). README.md:6 explicitly calls 00_bm.py the "legacy text→regex→CSV path." The --only-aggregate branch (lines 173-175) re-parses existing .log files and is the only still-runnable part. Two minor precisions, neither undermines the finding: (1) export writes a FIXED column header `stage,little,medium,big,vulkan,cuda,device,run` (line 51) whereas 00_bm.py passes through whatever columns the benchmark's CSV markers emit plus device/run — so they write to the same FILES but not necessarily identical column sets; the real duplication is "two implementations of benchmark-text→stage-CSV feeding the same z3 inputs," which holds. (2) The finding's deadcode category fits the run path; the file is not wholly dead since --only-aggregate works. Proposed fix (top-of-file banner, don't delete) aligns with the repo's stated policy of leaving pre-existing dead code with a mention.

### [python-collection] 05_timeline.py parses a log format the C++ no longer emits (wrong UID/frequency regex + nonexistent Chunk-annotation lines)  _(M/low, confirmed)_
- **evidence:** record.hpp:121-126 emits inside ### Python Begin ###: `Schedule_UID=<uid>` and `Frequency=<n> Hz` (no leading '#'), plus `Task=.. Chunk=.. Start=.. End=.. Duration=..` (record.hpp:132-136). But 05_timeline.py:78 matches `Schedule_UID: ([\w\-]+)` (colon) and 05_timeline.py:70 matches `# Frequency=(\d+) Hz` (hash prefix — that line, record.hpp:115, lives OUTSIDE the Begin/End block 05 first slices on). So UID extraction always returns None and frequency always falls back to the hardcoded 24576000 default (05:73), silently mis-scaling cycles->ms on any device with a different counter freq. 05:84-110 extract_schedule_annotations scans for `  Chunk N [..]: tasks` lines that record.hpp's python block never prints. By contrast 04's results/log_parser.py:59,67 correctly use `Schedule_UID=` and `Frequency=` — proof the two parsers disagree and 05 is the stale one.
- **claim:** Refines hypothesis (2): 05 is not merely 'latent issues', it is broken against the current emitter — frequencies default to a wrong constant and chunk annotations are dead. Any timeline figure produced is wrong off-Jetson or simply empty.
- **fix:** Align 05's regexes with record.hpp / results/log_parser.py (`Schedule_UID=`, `Frequency=`), drop the dead extract_schedule_annotations path, or have 05 import the parsing helpers from results/log_parser.py instead of maintaining a third copy.
- **verifier note:** Verified against real code; every cited line matches. record.hpp dump_records_for_python (builtin-apps/pipeline/record.hpp:120-142) emits, INSIDE the ### Python Begin/End ### block: `Schedule_UID=<uid>` (:125), `Frequency=<n> Hz` with no leading '#' (:126), and `Task=i Chunk=j Start=.. End=.. Duration=..` (:132-136). The `# Frequency=...` line is in a DIFFERENT function dump_records_metadata (:115), outside the block. 05_timeline.py first slices content to the Begin/End section (05:56-57), then: extract_schedule_uid matches `Schedule_UID: ([\\w\\-]+)` with a COLON (05:78) -> never matches the emitter's `=`, always returns None; extract_frequency matches `# Frequency=(\\d+) Hz` with a hash prefix (05:70) -> that '#'-prefixed line lives outside the sliced section, so it never matches and falls back to the hardcoded 24576000 default (05:73); extract_schedule_annotations scans for lines starting with two-space `  Chunk ` (05:90) which the python block never prints -> dead path. By contrast results/log_parser.py uses the correct `Schedule_UID=` (:59) and `Frequency=(\\d+) Hz` no-hash (:67), confirming the two parsers disagree and 05 is the stale one. Task-data parsing (05:118) does use the correct `Task=.. Chunk=..` pattern, so timelines aren't entirely empty off-Jetson, but cycle->ms scaling is wrong whenever the device counter frequency != 24576000, and UID/annotations are broken. Proposed fix (align regexes or import log_parser helpers) is sound. Minor caveat: on a device whose actual counter freq equals 24576000 the fallback is coincidentally correct, but the finding already scopes the bug to differing frequencies.

### [python-collection] 04 makespan path: widest-chunk avg has a swallowed-error time-window parser and a div-by-zero in stats when predicted=0  _(M/low, confirmed)_
- **evidence:** Two issues in the 04 chain. (a) parse_time_window (04_parse_schedules.py:73-92) wraps float() AND the range check in one try/except that prints and falls back to (0.0,1.0): a typo like `-t 0.2-0.8x` or `-t 0.9-0.1` is silently downgraded to the full window instead of erroring, so you analyze a different window than asked. (b) model_comparison.py:171 filters inf out of rel_differences_pct, then lines 225/228/231 divide by len(rel_differences_pct); if every matched prediction has max_time==0 (or list empty) this is a ZeroDivisionError that aborts the whole report after the heavy parse. Separately, extract_widest_chunks (statistics.py:132) seeds widest_chunk_duration=0, so a schedule whose chunks are all <=0 yields no widest chunk and is dropped from the comparison without warning.
- **claim:** The 04 statistics path is the load-bearing 'measured vs z3-predicted makespan' deliverable, yet it fails open (wrong window, silent drop) or fails hard (div-by-zero) on the degenerate inputs you actually hit when a device produces empty/zero records. The widest-chunk-as-makespan model is itself defensible (it matches solver T_max = bottleneck chunk, constraints.py/solution_analyzer.py:158), so the issue is robustness, not the metric choice.
- **fix:** Split the float-parse from the range-validation in parse_time_window and exit non-zero on a malformed window rather than redefining it. Guard model_comparison.py:225-231 with `if rel_differences_pct.size` (and report how many were dropped as inf). Have extract_widest_chunks initialize to -inf / track 'no positive chunk' explicitly.
- **verifier note:** All three sub-issues verified against real code. (a) parse_time_window (04_parse_schedules.py:73-92): a single try/except ValueError wraps BOTH float() parsing AND the range check (line 83-86: start<0||>1||end<0||>1||start>=end). On any failure it prints and returns (0.0,1.0). So an inverted window like `-t 0.9-0.1` raises at line 84, is swallowed, and silently analyzes the full window instead of erroring — real fail-open. Caveat: the evidence's `-t 0.2-0.8x` example actually fails in float() (the `x`), not the range check, but the same except still swallows it, so the point holds. (b) model_comparison.py: line 171 strips inf from rel_differences_pct; the error-distribution block (line 214) IS guarded by `if len>0`, but the prediction-accuracy block at lines 225/228/231 divides by len(rel_differences_pct) with NO guard. If every matched prediction has predicted==0 (line 161-164 -> inf, then filtered out), the array is empty -> ZeroDivisionError aborting the report after the heavy parse. The author's partial guard at 214 confirms they knew it could be empty but missed 225-231. Minor mischaracterization: the zero that triggers it is the z3 `predicted` value (line 161), not `max_time==0` as the evidence text states. (c) extract_widest_chunks (statistics.py:132): seeds widest_chunk_duration=0 with strict `>` (line 136); a schedule whose chunks are all avg_duration_ms<=0 leaves widest_chunk_id=None and is dropped at line 141 with no warning — real silent drop. The metric itself is defensible: solution_analyzer.py:150/158 defines predicted makespan as max_time=max(chunk_times)=bottleneck chunk, matching widest-chunk-as-measured-makespan, so this is genuinely a robustness issue not a metric error. Proposed fixes (split parse from range-validate and exit non-zero; guard with `if rel_differences_pct.size`; init widest to -inf / track no-positive-chunk) are sound. Effort M / risk low are reasonable.

### [python-collection] gen_schedule_tpu.py is an unreferenced stale duplicate of 02 that hardcodes minimize_mode='gapness'  _(S/low, confirmed)_
- **evidence:** gen_schedule_tpu.py shares 02_gen_schedule_merged.py's docstring and imports (lines 1-19) but at line 109 sets `minimize_mode = "gapness"` ignoring its own --minimize_mode arg (line 56), and at line 155 calls solve_optimization_problem(stage_timings, args.num_solutions, app) — same un-threaded call as 02. grep across scripts/ and docs/ finds zero references to gen_schedule_tpu (no justfile/run-on-* script invokes it); README.md:63 instead documents two OTHER nonexistent names (02_schedule.py, 02_schedule_using_normal_table.py). The 04_parse_schedules.py and 04_parse_schedules_adv.py files are also byte-identical (diff == empty).
- **claim:** Three orphan/duplicate 02/04 entrypoints (gen_schedule_tpu.py, 04_parse_schedules_adv.py, plus README pointing at vanished script names) make it ambiguous which schedule generator/parser is canonical, and the orphan bakes in the gapness-only bug so 'switching to it' silently loses tmax.
- **fix:** Mention/retire gen_schedule_tpu.py and the duplicate 04_parse_schedules_adv.py (keep one, note the other is a copy per repo policy), and fix README.md:63-69 to name the real entrypoint 02_gen_schedule_merged.py with its actual --csv_root_folder/--table_type/--minimize_mode flags.
- **verifier note:** Verified against the real files; the finding is accurate. /home/doremy/Desktop/ucsc/better-together/scripts/collect/gen_schedule_tpu.py shares 02_gen_schedule_merged.py's docstring+imports (lines 1-19 identical) and at line 109 hardcodes minimize_mode="gapness" (also hardcodes backend="vk", device="3A021JEHN02756", app="cifar-dense", table_type="btpm" at lines 105-108), ignoring its own --minimize_mode/--backend/--device/--app args declared in parse_arguments. Line 155 calls solve_optimization_problem(stage_timings, args.num_solutions, app) — same un-threaded signature as 02's line 158. grep across scripts/, docs/, justfile(.old), and READMEs finds ZERO references to gen_schedule_tpu — it is fully orphaned. 04_parse_schedules.py and 04_parse_schedules_adv.py are byte-identical (diff empty); _adv is also unreferenced (all callers — param.sh, print.py, 03_run_schedule.py, justfile.old, docs 06 — use 04_parse_schedules.py). The stale README is scripts/collect/README.md:63,69 (finding wrote 'README.md:63', minor path imprecision) which documents nonexistent 02_schedule.py / 02_schedule_using_normal_table.py; the real entrypoint 02_gen_schedule_merged.py is correctly referenced elsewhere (root README.md:313, smt/README.md:53, docs 06). Two corrections that make the orphan MORE dead than stated: (a) gen_schedule_tpu hardcodes far more than just minimize_mode, and (b) it is actually broken — line 164 passes baseline_data which is never assigned (the assignment block is commented out at lines 113-121), so the script raises NameError before writing output; thus 'switching to it' would not merely silently lose tmax, it would crash. Proposed fix (retire/note the duplicates, correct README flags to --csv_root_folder/--table_type/--minimize_mode) is appropriate and low-risk.

### [build-system] 18 bm-* targets hand-enumerated with the exact app-list the adjacent foreach() already loops over  _(M/low, confirmed)_
- **evidence:** CMakeLists.txt:177-191 (cu) and 257-272 (vk) hand-type bm-baseline/bm-fully/bm-gen-logs for {tree,cifar-dense,cifar-sparse}, while CMakeLists.txt:197-200 and 277-280 use `foreach(app tree cifar-dense cifar-sparse)` for bm-prof over the SAME `pipe/${app}-{cu,vk}/` layout. `ls pipe/*/` confirms all 6 cells are byte-uniform: each has exactly bm_baseline/bm_fully_vs_normal/bm_gen_log/bm_prof. The whole bm-gen-logs-tree-vk omission this session is the failure mode this invites: `git log -S bm-gen-logs-tree-vk` shows it landed in one commit (6b0a089) by hand-copy, the exact place a copy can drop a line.
- **claim:** The hand-enumeration is provably redundant — the canonical-source proof is in the same file, since bm-prof already loops the identical (app x backend) cartesian product. 18 lines that could be 2 foreach bodies; every new app or bm-kind must be added in 6 places by hand, and a dropped line builds fine and only fails at deploy.
- **fix:** Wrap the per-cell bm targets in `foreach(app tree cifar-dense cifar-sparse) foreach(kind baseline fully:bm_fully_vs_normal gen-logs:bm_gen_log ...)`. Better: one outer `foreach(backend cu vk)` driving both blocks, since bt_add_cuda_app/bt_add_vk_app differ only in source extension (.cu/.cpp) and link libs. The pipe/ layout is already regular enough to glob the cell list with `file(GLOB)` of `pipe/*-${suffix}` if dynamic discovery is wanted.
- **verifier note:** Core claim verified against CMakeLists.txt. Lines 177-191 (CUDA) and 257-272 (Vulkan) hand-enumerate exactly 18 pipe-based bm targets: bm-baseline/bm-fully/bm-gen-logs each × {tree,cifar-dense,cifar-sparse} × {cu,vk} = 18, over the pipe/${app}-{cu,vk}/ layout. Meanwhile lines 197-200 and 277-280 already use `foreach(app tree cifar-dense cifar-sparse)` for bm-prof over the IDENTICAL pipe/${app}-{cu,vk}/ layout — so the canonical-source proof genuinely sits in the same file. Confirmed all 18 source files exist and are uniform (bm_baseline/bm_fully_vs_normal/bm_gen_log present in every pipe/*-{cu,vk}/ cell). Git claim confirmed: `git log -S bm-gen-logs-tree-vk` returns exactly one commit 6b0a089 ("build(cmake): migrate bm-gen-logs..."), the hand-copy landing point. The redundancy, the "6 places per new app/kind" maintenance cost, and the dropped-line-builds-fine failure mode are all real. The proposed foreach refactor is sound; note the per-kind link-lib heterogeneity (baseline/fully use benchmark::benchmark, gen-logs uses nlohmann_json::nlohmann_json) means a combined foreach needs a kind→lib mapping, which the proposed fix already accounts for. Two minor evidence imprecisions, neither affecting the finding: (1) the finding says "all 6 cells are byte-uniform with exactly bm_baseline/bm_fully/bm_gen_log/bm_prof" — actually the -cu cells additionally contain main.cu (and const.hpp), and -vk cells contain const.hpp, so the directory listings are not literally identical; the bm_* files specifically ARE uniform, which is the relevant part. (2) The "this session's omission" framing is rhetorical — the line is currently present (line 270), so it was a hypothetical/historical risk, not a current defect. Effort M / risk low are reasonable; this is a maintainability cleanup, not a bug.

### [build-system] Missing-target omissions are silent at configure/build and only surface at scp-deploy time  _(S/low, overstated)_
- **evidence:** scripts/run-on-jetson.sh:31-33 and run-on-rocky.sh:26-32 build `paths+=("$BUILD/$t")` from caller-supplied target names then `scp "${paths[@]}"`. If a target was never enumerated in CMakeLists (e.g. the bm-gen-logs-tree-vk gap), `cmake --build` succeeds, the binary simply doesn't exist, and the only error is scp's 'No such file' on the device push. There is no `bm-all`/aggregate custom target and no check that the requested benchmark set is complete.
- **claim:** A hand-list omission has zero local signal — no missing-source error, no missing-target error — because CMake only errors on files you DO reference. The orchestrator discovers the gap remotely, mid-run, which is exactly what happened.
- **fix:** Generating the targets via foreach (finding #1) closes the omission class structurally. Additionally add an aggregate `add_custom_target(bm-gen-logs DEPENDS bm-gen-logs-tree-vk ...)` (itself foreach-populated) so `cmake --build --target bm-gen-logs` is the single deploy entry point and a missing cell is a configure-time error.
- **verifier note:** The structural/mechanical core of the finding is REAL and correctly characterized. In scripts/run-on-jetson.sh (lines 24-33) and scripts/run-on-rocky.sh (lines 70-79), `targets=("$@")` then `paths+=("$BUILD/$t")` then `scp "${paths[@]}" "$HOST:$DEST/"` — caller-supplied target names are turned into local paths and scp'd with NO existence check. There is no `add_custom_target` aggregate anywhere in CMakeLists.txt (confirmed by grep: only bt_add_*_app calls and per-target foreach), so no `bm-all`/`bm-gen-logs` umbrella exists and `cmake --build --target X` for an undeclared X is not a single deploy entry point. The claim that CMake only errors on referenced-but-missing sources (so a hand-list omission has zero configure/build signal and only surfaces as scp 'No such file' on the device) is accurate. The proposed foreach + aggregate fix would indeed convert a missing cell into a configure-time error.\n\nWhat is OVERSTATED: the concrete anecdote. The finding cites 'the bm-gen-logs-tree-vk gap' as the omitted target and says 'which is exactly what happened.' But in the current tree, bm-gen-logs-tree-vk IS enumerated — CMakeLists.txt:270 (bt_add_vk_app bm-gen-logs-tree-vk pipe/tree-vk/bm_gen_log.cpp ...), alongside all six bm-gen-logs-{tree,cifar-dense,cifar-sparse}-{cu,vk} targets (lines 189-191, 270-272). So the specific cited gap does not exist as a present-tense fact; either it was already fixed or it's a historical/hypothetical example. The robustness class is genuine, but the specific evidence pointer is not currently a live defect.

### [build-system] BT_GIT_SHA captured at configure time, so benchmark binaries stamp a stale sha after any commit without reconfigure  _(M/low, confirmed)_
- **evidence:** CMakeLists.txt:195-196 and 275-276 run `execute_process(COMMAND git rev-parse --short HEAD ...)` at configure time into BT_GIT_SHA, baked via target_compile_definitions (199, 279). pipe/bm_prof_common.hpp:164 writes `{"git_sha", BT_GIT_SHA}` into the canonical JSONL provenance. CMake does not re-run configure when HEAD moves, so committing and rebuilding (`cmake --build` only) leaves the OLD sha compiled into bm-prof-*. The provenance field that exists specifically to identify the measured code version can silently lie.
- **claim:** For a profiling store whose entire point is reproducible measured-vs-modeled provenance, a git_sha that doesn't track the actual built commit is a correctness defect, not cosmetic — runs across a commit boundary get mislabeled identically.
- **fix:** Capture the sha at BUILD time, not configure time: add a custom command / `add_custom_target` that re-runs `git rev-parse` and writes a generated header (or use a `.git/HEAD` + `.git/index` file dependency to force reconfigure). Minimally, document that bm-prof requires a fresh `cmake` reconfigure before each measurement campaign.
- **verifier note:** Verified against CMakeLists.txt:195-200 (CUDA) and 275-280 (Vulkan): execute_process runs `git rev-parse --short HEAD` at CONFIGURE time, baked via target_compile_definitions(BT_GIT_SHA="..."). bm_prof_common.hpp:164 writes that macro into the JSONL provenance git_sha field. grep confirms NO CONFIGURE_DEPENDS / .git/HEAD dependency exists in CMakeLists.txt or cmake/, so CMake does not re-run configure when HEAD moves; a plain `cmake --build` after a commit can recompile the prof TU and stamp the stale configure-time sha into provenance. The repo's own comments (lines 192-194, 273-274) admit 'stamped at configure time,' confirming this is the real design. Real, correctly characterized correctness defect; severity is low (provenance mislabel, not measurement corruption). Minor nuance: the stale-stamp only manifests when the bm_prof translation unit is actually recompiled — if its sources are untouched, the object isn't rebuilt at all — but the realistic kernel-edit-then-rebuild workflow triggers exactly the described mislabeling. Proposed fix (build-time sha capture via custom command/generated header, or a .git/HEAD reconfigure dependency) is appropriate.

### [build-system] git-sha capture and the CUDA compile-options string are duplicated verbatim across cu/vk blocks  _(S/low, confirmed)_
- **evidence:** `grep git rev-parse` -> identical execute_process at CMakeLists.txt:195 and 275 (comment at 192-194 even admits 'captured here too since the CUDA block precedes the Vulkan one'). `--diag-suppress=20012 -Xcompiler=-fopenmp` appears 3x (lines 146-147, 156-157, 321-322) as a copy-pasted generator-expression. The sha is invariant of backend, so capturing it twice is pure duplication.
- **claim:** Two copies of the same configure-time command and three copies of the same nvcc flag string drift independently; a change (e.g. adding a suppress code, or `--short=12`) must be made in 2-3 places and one will be missed.
- **fix:** Capture BT_GIT_SHA once near the top (before the CUDA block) so both backends read one variable. Factor the CUDA flags into a single `set(BT_CUDA_OPTS $<...>)` or an INTERFACE `bt_cuda_flags` target applied by bt_add_cuda_app and the test helper.
- **verifier note:** Verified against /home/doremy/Desktop/ucsc/better-together/CMakeLists.txt. Git SHA capture: lines 195-196 and 275-276 are byte-identical execute_process(COMMAND git rev-parse --short HEAD ... OUTPUT_VARIABLE BT_GIT_SHA ...), both inside BT_BUILD_BENCHMARKS blocks (CUDA at 176-201, Vulkan at 252-281). The comment at 192-194 explicitly admits the second capture exists 'since the CUDA block precedes the Vulkan one that also needs it.' Both write the same variable name, so naming can't drift, but flags can (the claim's example of --short=12 is exactly such a divergence). CUDA flag string '--diag-suppress=20012 -Xcompiler=-fopenmp' appears verbatim 3x: lines 146-147 (bt_cuda lib), 156-157 (bt_add_cuda_app fn), 321-322 (bt_add_cuda_test fn). All cited line numbers are exact. This is a genuine maintainability/duplication finding, correctly characterized and appropriately low-severity (not a correctness bug). Proposed fix (hoist BT_GIT_SHA once before the CUDA block; centralize CUDA opts) is sound. Minor extra: line 173 (test-omp) uses a partial variant '-Xcompiler=-fopenmp' only — a 4th near-copy that further supports the drift concern.

### [build-system] CUDA arch is specified in four places that can silently diverge from the toolchain hardcode  _(S/low, overstated)_
- **evidence:** BT_CUDA_ARCH default '87' at CMakeLists.txt:30; consumed via set_target_properties at 145,155,172,320; preset sets it '87' at CMakePresets.json:27; the jetson toolchain HARDCODES `set(CMAKE_CUDA_ARCHITECTURES 87)` at jetson-aarch64.cmake:22. The toolchain value is the global default but every target overrides it with BT_CUDA_ARCH, so building for an 89 (Ada) target by passing -DBT_CUDA_ARCH=89 still gets a toolchain that pins 87 as the CMAKE_CUDA_ARCHITECTURES default — the two knobs disagree and the comment '89=Ada' advertises a config that is half-wired.
- **claim:** The arch is the kind of value that must have exactly one source of truth; here a -DBT_CUDA_ARCH override is partially honored (per-target) while the toolchain default stays 87, an inconsistency that produces confusing PTX-arch behavior for any non-Orin target the comment invites.
- **fix:** Drop `set(CMAKE_CUDA_ARCHITECTURES 87)` from the toolchain (let the project's BT_CUDA_ARCH be the sole source), or have the toolchain read BT_CUDA_ARCH. Set the per-target property once on bt_cuda and PROPAGATE via the bt::cuda link (CUDA_ARCHITECTURES is not transitive, so a single INTERFACE helper or the bt_add_cuda_app/test helper should own it) rather than repeating it at 4 call sites.
- **verifier note:** All four citations are factually correct: CMakeLists.txt:30 sets BT_CUDA_ARCH "87" with comment "87=Orin, 89=Ada"; it is consumed via set_target_properties at lines 145, 155, 172, 320; CMakePresets.json:27 sets BT_CUDA_ARCH "87"; and jetson-aarch64.cmake:22 hardcodes set(CMAKE_CUDA_ARCHITECTURES 87). The arch value really is repeated across multiple places, and the toolchain hardcode is redundant — a legitimate maintainability/DRY smell (4 repeated set_target_properties call sites, plus a dead toolchain default). The proposed fix (drop the toolchain hardcode and own CUDA_ARCHITECTURES once) is reasonable.

But the finding's central severity claim is overstated/inaccurate on the mechanics. In CMake, CMAKE_CUDA_ARCHITECTURES only seeds the DEFAULT for each target's CUDA_ARCHITECTURES property at creation; an explicit set_target_properties(... CUDA_ARCHITECTURES "${BT_CUDA_ARCH}") overrides it. I verified that EVERY CUDA target here gets that explicit override: bt_cuda (145), all runners/benchmarks via bt_add_cuda_app (155), test-omp (172), and all CUDA tests via bt_add_cuda_test (320). No CUDA target falls through to the toolchain's 87 default. Therefore passing -DBT_CUDA_ARCH=89 IS fully honored across all built artifacts — there is no actual PTX-arch divergence or "half-wired" 89 build. The toolchain's 87 is simply a never-observed dead default, not a knob that silently overrides BT_CUDA_ARCH. The claim that "building for 89 ... still gets a toolchain that pins 87" producing "confusing PTX-arch behavior" does not materialize, because the toolchain default never wins over the per-target property.

Net: real as a code-cleanliness/redundancy finding (S effort, low risk), but the asserted functional defect (silent divergence / wrong PTX for non-Orin targets) is false — making the finding overstated.

### [build-system] Preset divergence: BT_BUILD_BENCHMARKS is ON only for jetson/android, OFF (unset) for the vulkan preset that runs on the MiniPC/rocky  _(S/low, overstated)_
- **evidence:** CMakePresets.json: jetson sets BT_BUILD_BENCHMARKS ON (line 29), android sets it ON (line 56), but the `vulkan` preset (lines 33-44) does NOT set it, so it falls back to the option default OFF (CMakeLists.txt:29). The orchestrator states they ran 'MiniPC(vk)' — i.e. the `vulkan` preset / BT_TEST_DEVICE=minipc — through the full pipeline, which requires bm-gen-logs-*-vk and bm-prof-*-vk. With this preset those targets are NOT built.
- **claim:** The one preset used for the MiniPC Vulkan profiling run builds zero benchmark binaries by default, so running the pipeline there requires remembering -DBT_BUILD_BENCHMARKS=ON out-of-band; nothing in the preset encodes the requirement, a latent foot-gun matching the 'ran end-to-end on MiniPC(vk)' workflow.
- **fix:** Add `"BT_BUILD_BENCHMARKS": "ON"` to the vulkan preset (and audit the pc preset — it's correctly OFF since it's the test-only gate). Or split presets into pc/vulkan (test) vs jetson-bench/vulkan-bench (profiling) so the benchmark intent is explicit per preset.
- **verifier note:** Core technical claim is REAL and correct: the `vulkan` preset (CMakePresets.json lines 34-43) does not set BT_BUILD_BENCHMARKS, so it inherits the option default OFF (CMakeLists.txt:29). The vk profiling targets a Vulkan pipeline run needs — bm-gen-logs-*-vk (CMakeLists.txt:270-272) and bm-prof-*-vk (line 278), plus all other bm-*-vk — are inside `if(BT_BUILD_BENCHMARKS)` (line 252), so the `vulkan` preset builds zero benchmark binaries. `jetson` correctly sets it ON (line 29). So profiling on the MiniPC via `--preset vulkan` does require an out-of-band -DBT_BUILD_BENCHMARKS=ON; nothing in the preset encodes it. The committed justfile reinforces this: `build-x86` uses `--preset vulkan` but builds only test-* binaries (omp_vk_bins, line 30/45), never bm-* targets.\n\nHOWEVER the finding is OVERSTATED/partly wrong in its evidence: it claims 'android sets it ON (line 56)' and that benchmarks are ON 'only for jetson/android'. FALSE — line 56 is `\"BT_TEST_DEVICE\": \"android\"`; the android preset does NOT set BT_BUILD_BENCHMARKS at all and inherits OFF, exactly like vulkan. Only the `jetson` preset sets it ON. So the symmetry argument ('jetson and android get it, vulkan doesn't') is incorrect — two of three non-pc presets (vulkan AND android) lack it. The pc preset correctly omits it (test-only gate), as the finding notes. The proposed fix (add BT_BUILD_BENCHMARKS ON to vulkan, or split test vs bench presets) is reasonable for the real underlying foot-gun, but the framing/line-citation for android is wrong.

### [structure-deadcode] utility/nnapi/ + two volk diagnostics are confirmed dead (not in CMake), but the others named in CLAUDE.md are NOT dead  _(S/low, confirmed)_
- **evidence:** CMakeLists.txt has a single explicit source list (no file(GLOB)); cross-referencing every utility/*.{cpp,cu} against it: UNUSED = utility/check_vulkan.cpp, utility/determine_cuda_kernel_block_size.cu, utility/query_warpsize.cpp, and all four utility/nnapi/*.cpp (bm_alexnet/test_alexnet/test_conv2d/test_nnapi). USED = bm_core_type.cpp, query_cpuinfo.cpp, query_vulkan_hpp_version.cpp, test_affinity.cpp, test_omp.cu, stress/stressor-omp.cpp, stress/stressor-vk.cpp. CMakeLists.txt:6-8 explicitly says volk diagnostics (check-vulkan/query-warpsize) + NNAPI 'still live under utility/ and can be re-added'.
- **claim:** Hypothesis (1) is only partially right. NNAPI (4 files, ~64KB incl. a 35KB test_alexnet.cpp) and the two unported volk probes (check_vulkan.cpp, query_warpsize.cpp) plus determine_cuda_kernel_block_size.cu are genuinely orphaned: not built, NNAPI not in any preset, and CLAUDE.md confirms NNAPI was never ported. But query_warpsize's function is now data-driven via gpu.subgroup_size in devices/*.json, so it is also conceptually superseded, not just unported. The rest of utility/ is live diagnostics and should stay.
- **fix:** Delete utility/nnapi/ wholesale (NNAPI was never ported and is superseded by the device-axis plan). For check_vulkan.cpp/query_warpsize.cpp/determine_cuda_kernel_block_size.cu, either re-add the 3 CMake targets the comment promises or delete them; keeping unbuildable source behind an aspirational comment is the worst of both. query_warpsize is redundant with the new subgroup_size spec field.
- **verifier note:** Verified against the real code. CMakeLists.txt uses a single explicit source list (no file(GLOB); comment at line 16). Grepping every utility/*.{cpp,cu} against the build: the four nnapi/*.cpp (bm_alexnet 18710B, test_alexnet 35318B, test_conv2d, test_nnapi), check_vulkan.cpp, query_warpsize.cpp, and determine_cuda_kernel_block_size.cu are referenced NOWHERE in CMakeLists.txt, cmake/, or CMakePresets.json — only mentioned in the prose comment at CMakeLists.txt:6-8 ("the volk diagnostics (check-vulkan / query-warpsize) and the NNAPI/EdgeTPU targets ... still live under utility/ and can be re-added here when needed"). The 7 USED files are all confirmed present in the explicit list: test_affinity.cpp:103, query_cpuinfo.cpp:105, stressor-omp.cpp:106-107, bm_core_type.cpp:121, test_omp.cu:171, query_vulkan_hpp_version.cpp:215, stressor-vk.cpp:248-249. The supersession claim also holds: query_warpsize.cpp prints subgroupProperties.subgroupSize (line 61), which is exactly what every devices/*.json now stores as gpu.subgroup_size. NNAPI-never-ported claim matches CLAUDE.md. One trivial wording slip: the title says "two volk diagnostics," but the genuinely-dead non-nnapi set is three files — the two volk probes (check_vulkan, query_warpsize) PLUS the CUDA probe determine_cuda_kernel_block_size.cu (not a volk diagnostic). The body lists all three correctly, so this is cosmetic and does not affect substance. Finding is real and correctly characterized; the proposed fix (delete nnapi/ wholesale; for the 3 probes either add the promised CMake targets or delete) is sound.

### [structure-deadcode] The real 'duplication farm' is pipe/, not builtin-apps/*/dispatchers — 6 app×backend cells each re-implement an identical pipeline harness  _(M/medium, confirmed)_
- **evidence:** pipe/ has 6 dirs (tree-{cu,vk}, cifar-dense-{cu,vk}, cifar-sparse-{cu,vk}) each with bm_baseline/bm_fully_vs_normal/bm_gen_log/bm_prof(+main for cu)/const.hpp. pipe/tree-vk/const.hpp (111 lines) vs pipe/cifar-dense-vk/const.hpp differ by only 10 lines; the cu pair by 12. The differing lines are exactly `constexpr size_t kNumStages`, `using DispatcherT=...`, `using AppDataT=...`; the worker()/worker_with_record()/make_dataset()/make_queue_from_vector() bodies (const.hpp:26-111) are byte-identical across cells. bm_gen_log across apps: 95 differing lines out of 257. This is the per-cell type duplication referenced in the prompt.
- **claim:** Hypothesis (2) is correct in spirit but mislocated. The incremental path toward rearchitecture.md's Stage/Kernel registry (Phase 4: 'delete the pipe/ duplication farm') is templating const.hpp's harness over <AppDataT, DispatcherT, kNumStages>. These bodies are pure harness (queue plumbing, SPSC workers, dataset alloc) — the 'everything except the kernel bodies' dedup floor the doc itself names. The kernel math (.comp/.cu/omp loops) correctly stays per-cell.
- **fix:** Smallest high-value step: lift const.hpp into one builtin-apps/pipeline/cell.hpp templated on AppDataT/DispatcherT/kNumStages (a single Traits struct per cell supplies the 3 differing symbols). That collapses 6×111 lines to 1 template + 6 tiny traits headers and is mechanical/test-guarded (per-stage ctest already green). Do this before the full Stage/Kernel registry — it is the 80% dedup win at 20% of the risk.
- **verifier note:** Verified against /home/doremy/Desktop/ucsc/better-together/pipe/. The 6 cells (tree-{cu,vk}, cifar-dense-{cu,vk}, cifar-sparse-{cu,vk}) each carry the claimed file set: bm_baseline/bm_fully_vs_normal/bm_gen_log/bm_prof/const.hpp, plus main.cu only for the cu cells. const.hpp is 111/112 lines in every cell. The vk pair (tree-vk vs cifar-dense-vk) differs by exactly 10 lines and the cu pair by exactly 12 — matching the claim — and the differing lines are precisely the dispatcher includes plus `constexpr size_t kNumStages`, `using DispatcherT=...`, `using AppDataT=...`. const.hpp (lines 26-111) is pure harness: make_dataset/make_queue_from_vector (SPSC plumbing) + worker()/worker_with_record() SPSC workers. The 3 vk cells' bodies are byte-identical (identical md5). rearchitecture.md (docs/reports-for-human/rearchitecture.md:96) genuinely lists Phase 4: 'Stage/Kernel/Backend registry... delete the pipe/ duplication farm', so the finding's doc characterization is accurate and the proposed Traits/template lift is a faithful incremental step toward it.

Two minor mischaracterizations that do NOT undermine the thesis: (1) the cu cells' bodies are NOT byte-identical — they differ by one line (line 32: cifar cells hardcode `make_unique<cifar_dense::AppData>`/`<cifar_sparse::AppData>` while tree-cu uses the `AppDataT` alias). Off-by-one, and itself just a type-name that the proposed Traits approach unifies. (2) 'bm_gen_log: 95 differing lines out of 257' is not reproducible — actual pairwise diffs are 10 lines (cu) to 27 lines (vk) out of 242-257; if anything bm_gen_log is MORE duplicated than stated, so the wrong figure cuts in the finding's favor. Core claim (pipe/ harness duplication, templatable over <AppDataT,DispatcherT,kNumStages>, endorsed by the doc) is real and correctly located.

### [structure-deadcode] device_specs_embedded.hpp is a checked-in, hand-regenerated artifact with NO build-time regeneration — comment in conf.cpp is wrong and it can silently drift from devices/*.json  _(S/low, confirmed)_
- **evidence:** conf.cpp:9-11 claims specs are 'embedded at build time into device_specs_embedded.hpp via scripts/embed_device_specs.py, so there is no runtime file dependency'. But: (a) `git ls-files` shows builtin-apps/device_specs_embedded.hpp is TRACKED; (b) grep of CMakeLists.txt for embed/add_custom_command/device_specs_embedded returns nothing — there is no codegen step; (c) the header top says 'AUTO-GENERATED ... DO NOT EDIT. Regenerate after changing any devices/*.json'. So a dev edits devices/<id>.json, forgets to run scripts/embed_device_specs.py, and the C++ build happily compiles stale topology with zero warning. This is exactly the 'README vs conf.cpp drift' class the device axis was meant to kill, reintroduced one layer down.
- **claim:** The Phase-1/2 goal ('single source of truth', 'add a device = drop a file, no recompile') is undermined: there are now TWO sources (devices/*.json and the embedded .hpp) kept in sync by a manual step, and the code comment misdescribes the mechanism as automatic.
- **fix:** Add a CMake add_custom_command that runs scripts/embed_device_specs.py with devices/*.json as DEPENDS and the .hpp as OUTPUT, wired into bt_core's sources (or generate into the build tree and stop tracking the .hpp). Failing that, add a CI/ctest check that re-runs the script and `git diff --exit-code`s the header. Fix the conf.cpp:9 comment to say 'embedded at *generation* time (manual)' until the codegen exists.
- **verifier note:** All factual claims verified against the real code. (a) builtin-apps/device_specs_embedded.hpp is git-tracked (git ls-files lists it). (b) No codegen step exists: grep for embed_device_specs / device_specs_embedded / add_custom_command across the real CMakeLists.txt + cmake/ returns nothing; the only CMake hits are unrelated third-party files under build/_deps. scripts/embed_device_specs.py exists but is invoked only manually. (c) The header banner reads verbatim "AUTO-GENERATED from devices/*.json by scripts/embed_device_specs.py -- DO NOT EDIT. Regenerate after changing any devices/*.json." conf.cpp lines 8-14 say the specs are "embedded at build time into device_specs_embedded.hpp ... so there is no runtime file dependency" — the "at build time" clause is the misdescription, since embedding actually happens at generation time via a manual script run, not during the CMake build. So there are effectively two in-sync-by-hand sources (devices/*.json and the .hpp), and nothing in the build compares them, so editing a device JSON and forgetting to regen compiles stale topology with no warning. This does reintroduce the README-vs-conf.cpp drift class one layer down. Minor nuance: the conf.cpp comment is not wholly wrong — its final sentence does instruct devs to "regenerate the embedded header," acknowledging a manual step — but that contradicts its own "at build time" wording, which is the core defect the finding flags. Verdict: real, correctly characterized; proposed fixes (CMake add_custom_command with DEPENDS devices/*.json + OUTPUT the .hpp, or a CI git-diff --exit-code check, plus a comment correction) are appropriate. Effort/risk (S/low) reasonable.

### [structure-deadcode] rearchitecture.md is stale: it says Phase 2 (C++ device loader) is 'Next', but it is already DONE  _(S/low, confirmed)_
- **evidence:** rearchitecture.md:94 lists Phase 2 ('DeviceRegistry reads devices/*.json at runtime; delete the hardcoded table in conf.cpp') as not-yet-done, and §5 (lines 99-111) lists only Phase 1 under 'Done' with Phase 2 under 'Next'. But conf.cpp:26-47 already implements DeviceRegistry() by parsing the embedded JSON specs (the hardcoded C++ table is gone), conf.hpp:94-107 is the data-driven registry, and app.cpp:7-16 derives get_vulkan_warp_size() from gpu.subgroup_size. The doc's own north-star is behind the code.
- **claim:** The single load-bearing design doc misrepresents project state, which will mislead the next contributor (human or AI) into re-doing finished work or distrusting the doc. Per CLAUDE.md this doc is where 'why/where we are' lives, so its accuracy is load-bearing.
- **fix:** Move Phase 2 to 'Done' in §4/§5, note the one caveat that the loader reads an embedded header rather than the files at runtime (so 'no recompile' is not literally true yet — see the drift finding), and re-point 'Next' at Phase 3 (profiling-table/schedule contracts) which the schedule_source/config_reader code shows is the actual current gap.
- **verifier note:** Verified against the real code. rearchitecture.md:94 lists Phase 2 ("DeviceRegistry reads devices/*.json at runtime; delete the hardcoded table in conf.cpp") in the migration table, and §5 (lines 99-111) lists ONLY Phase 1 under "Done" with Phase 2 explicitly under "Next". But the code shows Phase 2 is already implemented: conf.cpp:26-47 implements DeviceRegistry() by parsing embedded JSON specs (bt::device_specs::kEmbedded) into the devices_ map — the old hardcoded C++ table is gone (the comment at conf.cpp:8-14 confirms "now data-driven... replaces the old hand-maintained C++ table"). conf.hpp:94-107 is the data-driven registry. app.cpp:7-16 derives get_vulkan_warp_size() from gpu.subgroup_size via the registry, with a comment "Adding a board is now dropping a JSON spec, not editing a hardcoded map here." So the doc's stated state is behind the code; the finding is real and correctly characterized. The finding also correctly flags the one caveat: the doc says "at runtime," but conf.cpp:6-14 reads device_specs_embedded.hpp generated at build time (embed_device_specs.py), so "no runtime file dependency" — meaning "add a device = drop a file, no recompile" is not literally true yet (a rebuild is needed to regenerate the header). This is a maintainability/staleness issue in the single load-bearing design doc, consistent with CLAUDE.md noting devices/*.json is now the source of truth (not conf.cpp). I did not independently verify the proposed-fix's claim about schedule_source/config_reader being the Phase 3 gap, but that is part of the suggested remedy, not the core finding.

### [structure-deadcode] Orphaned predecessor entrypoints + duplicate worker harness left behind by the pipe/ migration (pre-existing dead code, not yours)  _(S/low, confirmed)_
- **evidence:** Exact-path CMake check shows these source files are NOT in any target: builtin-apps/cifar-dense/vulkan/bm_for_table.cpp, bm_real_schedule.cpp, bm_gen_log.cpp (only bm_main.cpp is built), builtin-apps/tree/vulkan/tmp.cpp, builtin-apps/tree/vulkan/bm_mini.cpp, builtin-apps/tree/omp/bm_mini.cpp. Crucially, pipeline/worker.hpp (worker_thread/worker_thread_record templates) is referenced by NOTHING in the live build — its only includer is the orphan bm_for_table.cpp; the live pipe/ cells reimplement the same logic as worker()/worker_with_record() in const.hpp. So pipeline/worker.hpp is dead-by-transitivity and duplicated.
- **claim:** The pipe/ rewrite superseded the per-app vulkan bm_* files and pipeline/worker.hpp but they were never removed, so the tree now carries two generations of the harness. This is structural debt that obscures which path is canonical (e.g. a reader can't tell bm_main vs bm_real_schedule vs pipe/.../bm_prof apart). Per CLAUDE.md these are pre-existing — flag, don't delete in unrelated work.
- **fix:** As a dedicated cleanup commit: delete the un-wired builtin-apps/*/vulkan/bm_for_table|bm_real_schedule|bm_gen_log and the *_mini/tmp orphans, and delete pipeline/worker.hpp once the pipe/ const.hpp worker is confirmed its only successor. Verify each is unreferenced (grep + a clean configure) before removal; ctest -L omp is unaffected since none are built.
- **verifier note:** Verified against the real tree. All six cited source orphans exist and are absent from CMakeLists.txt: builtin-apps/cifar-dense/vulkan/{bm_for_table.cpp, bm_real_schedule.cpp, bm_gen_log.cpp}, builtin-apps/tree/vulkan/{tmp.cpp, bm_mini.cpp}, builtin-apps/tree/omp/bm_mini.cpp. In those builtin-apps dirs only bm_main.cpp/run_main.cpp/dispatchers.cpp/test_main.cpp are wired; the CMake targets named bm-gen-logs-* and bm-baseline-* point at pipe/*/bm_gen_log.{cpp,cu} and pipe/*/bm_baseline.{cpp,cu}, NOT the builtin-apps copies. The worker.hpp claim is correct in substance but the PATH is wrong: it is builtin-apps/pipeline/worker.hpp, not pipeline/worker.hpp (no top-level pipeline/ dir exists; the live tree is pipe/). worker.hpp defines worker_thread/worker_thread_record; a repo-wide grep shows worker.hpp is #included by nothing, and the worker_thread symbol appears only in worker.hpp (def) and the orphan bm_for_table.cpp — which itself cannot even build since it #includes a nonexistent common.hpp. So worker.hpp is dead-by-transitivity, as claimed. The duplication claim holds: pipe/*/const.hpp reimplement the same harness as worker()/worker_with_record() (the record variant wraps func(app) in logger.start_tick/end_tick), and these are what the live pipe/*/bm_gen_log cells use. Two minor corrections/additions: (1) the cited worker.hpp path should be builtin-apps/pipeline/worker.hpp; (2) the finding under-counts orphans — builtin-apps/cifar-dense/vulkan/bm_baseline.cpp is ALSO an unwired orphan (CMake uses pipe/'s bm_baseline instead). CLAUDE.md guidance to flag-not-delete in unrelated work is apt; ctest -L omp is unaffected since none build.
