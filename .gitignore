# Project specific
# ppl/vulkan/shaders/compiled_shaders/*.spv

tmp/
tmp1/
tmp2/
tmp3/
tmp4/
justfile.bak

.venv-cifar/

# Resources
resources.tar.gz
resources/weights_npy/
resources/octomap/


# resources/resources.zip
*.zip

# Xmake cache
.xmake/
build/

# MacOS Cache
.DS_Store

.vscode/
.cache/
vsxmake2022/
compile_commands.json
.clangd/

*.nsys-rep

__pycache__/
termux-app_v0.118.1+github-debug_arm64-v8a.apk


cifar-10-batches-py/
*.sqlite
.venv/
venv/
# Experiment artifacts: profiling tables, z3 schedules, execution/benchmark logs,
# and the CIFAR dataset — all regenerable (BT-Profiler → export → z3 → run). The
# canonical results live as a release artifact / external store, not in git history.
data/

# Trained checkpoints (~137MB; regenerable via scripts/data_prep/alexnet_cifar10.py)
saved_params/
# CTest-generated artifacts
Testing/
# Local Claude Code settings (shared agents/commands are committed; personal config is not)
.claude/settings.local.json

# refactor behavior-preservation baseline (scripts/refactor_gate.sh)
.refactor-gate/
